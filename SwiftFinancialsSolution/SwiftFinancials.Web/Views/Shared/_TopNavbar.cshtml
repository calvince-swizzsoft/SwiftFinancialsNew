<div class="row border-bottom">
    <nav class="navbar navbar-static-top white-bg" role="navigation" style="margin-bottom: 0">
        <div class="navbar-header">
            <a class="navbar-minimalize minimalize-styl-2 btn btn-outline-info" href="#"><i class="fa fa-bars"></i> </a>
        </div>
        <ul class="nav navbar-top-links navbar-right">
            <li style="margin-right: 40px;">
                <div class="dropdown hyper-realistic-dropdown">
                    <button class="btn btn-outline-info dropdown-toggle stunning-3d-button" type="button" id="dropdownMenuButton" style="color: black;">
                        <span>--- Tools ---</span>
                    </button>
                    <div class="dropdown-menu stunning-3d-dropdown" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#modal1">Calculator</a>
                        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#modal2">To-Do List</a>
                        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#modal3">Loan Calculator</a>
                    </div>
                </div>
            </li>
            <li>
                <form id="logoutForm" method="post" action="@Url.Action("LogOff", "Account", new { Area = ""})">
                    <a href='#' onclick='this.parentNode.submit(); return false;'><i class="fa fa-sign-out"></i>Log out</a>
                </form>
            </li>
        </ul>
    </nav>
</div>



<div class="modal fade stunning-3d-modal" id="modal1" tabindex="-1" role="dialog" aria-labelledby="modal1Label" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal1Label">Calculator</h5>
                <button type="button" class="close text-danger" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <center>
                    <div class="calculator">
                        <div class="display">
                            <input type="text" id="display" disabled>
                        </div>
                        <div class="buttons">
                             Standard Buttons 
                            <button class="btn" id="clear">C</button>
                            <button class="btn" id="delete" style="color:white; background-color: red;">Del</button>
                            <button class="btn">/</button>
                            <button class="btn">*</button>
                            <button class="btn">7</button>
                            <button class="btn">8</button>
                            <button class="btn">9</button>
                            <button class="btn">-</button>
                            <button class="btn">4</button>
                            <button class="btn">5</button>
                            <button class="btn">6</button>
                            <button class="btn">+</button>
                            <button class="btn">1</button>
                            <button class="btn">2</button>
                            <button class="btn">3</button>
                            <button class="btn" id="equals">=</button>
                            <button class="btn">0</button>
                            <button class="btn">.</button>
                             Scientific Buttons 
                            <button class="btn" id="sci-toggle">Scientific</button>
                            <button class="btn" id="sqrt">√</button>
                            <button class="btn" id="pow">^</button>
                            <button class="btn" id="sin">sin</button>
                            <button class="btn" id="cos">cos</button>
                            <button class="btn" id="tan">tan</button>
                            <button class="btn" id="log">log</button>
                            <button class="btn" id="ln">ln</button>
                            <button class="btn" id="pi">π</button>
                            <button class="btn" id="exp">e^x</button>
                            <button class="btn" id="factorial">x!</button>
                        </div>
                    </div>
                </center>
            </div>
        </div>
    </div>
</div>


<div class="modal fade stunning-3d-modal" id="modal2" tabindex="-1" role="dialog" aria-labelledby="modal2Label" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal2Label">Modal 2</h5>
                <button type="button" class="close text-danger" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Content for Modal 2
            </div>
        </div>
    </div>
</div>


<div class="modal fade stunning-3d-modal" id="modal3" tabindex="-1" role="dialog" aria-labelledby="modal3Label" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal3Label">Loan Calculator</h5>
                <button type="button" class="close text-danger" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="loan-calculator">
                    <div class="calculator-inputs">
                        <label for="loanAmount">Loan Amount:</label>
                        <input type="number" id="loanAmount" placeholder="Enter loan amount" required>

                        <label for="interestRate">Annual Interest Rate (%):</label>
                        <input type="number" id="interestRate" placeholder="Enter interest rate" required>

                        <label for="loanTerm">Loan Term (Years):</label>
                        <input type="number" id="loanTerm" placeholder="Enter loan term" required>

                        <label for="paymentFrequency">Payments per Year:</label>
                        <select id="paymentFrequency">
                            <option value="12">Monthly</option>
                            <option value="4">Quarterly</option>
                            <option value="1">Annually</option>
                        </select>

                        <label for="interestMode">Interest Calculation Mode:</label>
                        <select id="interestMode">
                            <option value="reducing">Reducing Balance (Amortized)</option>
                            <option value="simple">Simple Interest</option>
                            <option value="compound">Compound Interest</option>
                            <option value="flat">Flat Rate Interest</option>
                            <option value="daily">Daily Interest Calculation</option>
                            <option value="bullet">Bullet Repayment</option>
                        </select>

                        <button onclick="calculateLoan()" class="calculate-btn">Calculate</button>
                    </div>

                    <div id="schedule" class="scrollable-table">
                        <h3 style="margin-top: 40px; text-align: center;">Repayment Schedule</h3>
                        <br />
                        <table>
                            <thead>
                                <tr>
                                    <th>Payment #</th>
                                    <th>Date</th>
                                    <th>Beginning Balance</th>
                                    <th>Payment</th>
                                    <th>Principal</th>
                                    <th>Interest</th>
                                    <th>Ending Balance</th>
                                </tr>
                            </thead>
                        </table>
                        <div class="scrollable-tbody">
                            <table>
                                <tbody id="repaymentSchedule">
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


<style>
    .stunning-3d-button {
        background: linear-gradient(145deg, #e6e6e6, #ffffff);
        box-shadow: 4px 4px 8px #b8b8b8, -4px -4px 8px #ffffff;
        border-radius: 12px;
        transition: all 0.3s ease;
    }

        .stunning-3d-button:hover {
            transform: scale(1.05);
            box-shadow: 6px 6px 12px #b8b8b8, -6px -6px 12px #ffffff;
        }

    .hyper-realistic-dropdown:hover .stunning-3d-dropdown {
        display: block;
    }

    .stunning-3d-dropdown {
        display: none;
        background: linear-gradient(145deg, #e6e6e6, #ffffff);
        box-shadow: 6px 6px 12px #b8b8b8, -6px -6px 12px #ffffff;
        border-radius: 12px;
        padding: 10px;
        transition: all 0.3s ease;
    }

        .stunning-3d-dropdown .dropdown-item {
            padding: 10px;
            color: #007bff;
        }

            .stunning-3d-dropdown .dropdown-item:hover {
                background: #007bff;
                color: white;
                border-radius: 8px;
            }


    .stunning-3d-modal .modal-dialog {
        transform: perspective(1000px) rotateX(10deg);
        background: #f9f9f9;
        border-radius: 15px;
        box-shadow: 10px 10px 30px rgba(0, 0, 0, 0.3);
        transition: all 0.5s ease;
    }

    .stunning-3d-modal .modal-content {
        background: linear-gradient(145deg, #f1f1f1, #e0e0e0);
        box-shadow: 8px 8px 16px rgba(0, 0, 0, 0.2), -8px -8px 16px rgba(255, 255, 255, 0.1);
    }

    .stunning-3d-modal .modal-header, .stunning-3d-modal .modal-footer {
        background: linear-gradient(145deg, #e0e0e0, #ffffff);
        border-radius: 12px;
    }

        .stunning-3d-modal .modal-header h5 {
            font-weight: bold;
            color: #007bff;
        }


    .stunning-3d-modal:hover .modal-dialog {
        transform: perspective(1000px) rotateX(0deg);
        box-shadow: 12px 12px 24px rgba(0, 0, 0, 0.4);
    }

    .modal {
        width: 80%;
    }



    /*Loan Calculator*/
    .loan-calculator {
        background: linear-gradient(145deg, #e0e0e0, #ffffff);
        padding: 20px;
        box-shadow: 8px 8px 16px rgba(0, 0, 0, 0.2), -8px -8px 16px rgba(255, 255, 255, 0.1);
        border-radius: 15px;
        width: 100%;
        margin: 20px auto;
    }

    .calculator-inputs {
        display: flex;
        flex-direction: column;
    }

        .calculator-inputs label {
            font-size: 14px;
            margin-top: 10px;
            color: #007bff;
        }

        .calculator-inputs input, select {
            padding: 10px;
            border: none;
            border-radius: 10px;
            background: #f0f0f0;
            box-shadow: inset 4px 4px 8px #b8b8b8, inset -4px -4px 8px #ffffff;
            font-size: 16px;
            margin-bottom: 10px;
        }

    .calculate-btn {
        background: #007bff;
        color: white;
        border: none;
        border-radius: 10px;
        padding: 10px;
        box-shadow: 4px 4px 8px rgba(0, 0, 0, 0.2);
        transition: 0.3s ease;
        font-size: 16px;
    }

        .calculate-btn:hover {
            background: #0056b3;
            transform: scale(1.05);
        }

    .scrollable-tbody {
        max-height: 300px; /* Adjust height as needed */
        overflow-y: auto;
    }

    .scrollable-table table {
        width: 100%;
        border-collapse: collapse;
    }

        .scrollable-table table th, .scrollable-table table td {
            padding: 10px;
            text-align: center;
            background: #f9f9f9;
            border-bottom: 1px solid #ddd;
        }

        .scrollable-table table th {
            background: #007bff;
            color: white;
        }

        .scrollable-table table tr:nth-child(even) td {
            background: #f0f8ff;
        }



    /*Calculator*/
    .calculator {
        width: fit-content;
        height: fit-content;
        background: #333;
        border-radius: 15px;
        box-shadow: 0 15px 25px rgba(0, 0, 0, 0.3);
        perspective: 1000px;
        transform-style: preserve-3d;
    }

    .display {
        padding: 20px;
        background: #222;
        color: #fff;
        font-size: 2em;
        border-radius: 15px 15px 0 0;
        box-shadow: inset 0 -2px 5px rgba(0, 0, 0, 0.5);
    }

    .buttons {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        grid-gap: 10px;
        padding: 10px;
    }

    /* .btn {
        background: #444;
        color: #fff;
        border: none;
        border-radius: 5px;
        font-size: 1.2em;
        padding: 15px;
        cursor: pointer;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        transition: transform 0.1s ease-in-out;
    }*/

    .btn:hover {
        transform: scale(1.1);
    }

    .btn:active {
        transform: scale(1);
    }

    #sci-toggle {
        grid-column: span 4;
        background: #555;
    }
</style>


<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
    function calculateLoan() {
        let loanAmount = parseFloat(document.getElementById('loanAmount').value);
        let annualInterestRate = parseFloat(document.getElementById('interestRate').value) / 100;
        let loanTerm = parseFloat(document.getElementById('loanTerm').value);
        let paymentsPerYear = parseInt(document.getElementById('paymentFrequency').value);
        let interestMode = document.getElementById('interestMode').value;

        if (loanTerm <= 0 || paymentsPerYear <= 0) {
            alert("Loan term and payments per year must be greater than 0.");
            return;
        }

        let numberOfPayments = loanTerm * paymentsPerYear;
        let monthlyInterestRate = annualInterestRate / paymentsPerYear;
        let schedule = [];
        let remainingBalance = loanAmount;
        let paymentDate = new Date();
        let monthlyPayment;

        switch (interestMode) {
            case 'reducing':
                // Reducing Balance (Amortized) Mode
                monthlyPayment = loanAmount * (monthlyInterestRate * Math.pow(1 + monthlyInterestRate, numberOfPayments)) / (Math.pow(1 + monthlyInterestRate, numberOfPayments) - 1);
                break;
            case 'simple':
                // Simple Interest Mode
                let totalInterestSimple = loanAmount * annualInterestRate * loanTerm;
                monthlyPayment = (loanAmount + totalInterestSimple) / numberOfPayments;
                break;
            case 'compound':
                // Compound Interest Mode
                let totalAmountCompound = loanAmount * Math.pow((1 + monthlyInterestRate), numberOfPayments);
                monthlyPayment = totalAmountCompound / numberOfPayments;
                break;
            case 'flat':
                // Flat Rate Interest Mode
                let totalInterestFlat = loanAmount * annualInterestRate * loanTerm;
                monthlyPayment = (loanAmount + totalInterestFlat) / numberOfPayments;
                break;
            case 'daily':
                // Daily Interest Calculation Mode
                let dailyInterestRate = annualInterestRate / 365;
                let totalInterestDaily = loanAmount * dailyInterestRate * (loanTerm * 365);
                monthlyPayment = (loanAmount + totalInterestDaily) / numberOfPayments;
                break;
            case 'bullet':
                // Bullet Repayment Mode
                monthlyPayment = loanAmount * monthlyInterestRate; 
                break;
            default:
                alert("Invalid interest mode selected.");
                return;
        }

        for (let i = 1; i <= numberOfPayments; i++) {
            let interestPayment, principalPayment;

            switch (interestMode) {
                case 'reducing':
                    // Reducing Balance calculation
                    interestPayment = remainingBalance * monthlyInterestRate;
                    principalPayment = monthlyPayment - interestPayment;
                    break;
                case 'simple':
                    // Simple Interest calculation
                    interestPayment = (loanAmount * annualInterestRate) / paymentsPerYear;
                    principalPayment = monthlyPayment - interestPayment;
                    break;
                case 'compound':
                    // Compound Interest calculation
                    interestPayment = remainingBalance * monthlyInterestRate;
                    principalPayment = monthlyPayment - interestPayment;
                    break;
                case 'flat':
                    // Flat Rate Interest calculation
                    interestPayment = (loanAmount * annualInterestRate) / paymentsPerYear;
                    principalPayment = monthlyPayment - interestPayment;
                    break;
                case 'daily':
                    // Daily Interest calculation
                    interestPayment = remainingBalance * (annualInterestRate / 365) * (365 / paymentsPerYear);
                    principalPayment = monthlyPayment - interestPayment;
                    break;
                case 'bullet':
                    // Bullet Repayment calculation (interest only payments)
                    interestPayment = loanAmount * monthlyInterestRate;
                    principalPayment = 0;
                    if (i === numberOfPayments) { 
                        principalPayment = loanAmount;
                        monthlyPayment = principalPayment + interestPayment;
                    }
                    break;
            }

            let endingBalance = remainingBalance - principalPayment;

            if (endingBalance < 0) {
                endingBalance = 0;
                principalPayment = remainingBalance;
                monthlyPayment = principalPayment + interestPayment;
            }

            schedule.push({
                paymentNumber: i,
                date: new Date(paymentDate.setMonth(paymentDate.getMonth() + (12 / paymentsPerYear))).toLocaleDateString(),
                beginningBalance: remainingBalance.toFixed(2),
                payment: monthlyPayment.toFixed(2),
                principal: principalPayment.toFixed(2),
                interest: interestPayment.toFixed(2),
                endingBalance: endingBalance.toFixed(2)
            });

            remainingBalance = endingBalance;

            if (remainingBalance <= 0) break;
        }

        displaySchedule(schedule);
    }

    function displaySchedule(schedule) {
        const tableBody = document.getElementById('repaymentSchedule');
        tableBody.innerHTML = '';

        schedule.forEach(row => {
            const newRow = document.createElement('tr');
            newRow.innerHTML = `
        <td>${row.paymentNumber}</td>
        <td>${row.date}</td>
        <td>${row.beginningBalance}</td>
        <td>${row.payment}</td>
        <td>${row.principal}</td>
        <td>${row.interest}</td>
        <td>${row.endingBalance}</td>
    `;
            tableBody.appendChild(newRow);
        });
    }







    //Calculator
    document.addEventListener('DOMContentLoaded', function () {
        const display = document.getElementById('display');
        const buttons = Array.from(document.querySelectorAll('.btn'));
        const sciToggle = document.getElementById('sci-toggle');
        const scientificButtons = Array.from(document.querySelectorAll('#sqrt, #pow, #sin, #cos, #tan, #log, #ln, #pi, #exp, #factorial'));
        let isScientific = false;

        buttons.forEach(button => {
            button.addEventListener('click', function () {
                if (button.id === 'clear') {
                    display.value = '';
                } else if (button.id === 'delete') {
                    display.value = display.value.slice(0, -1);
                } else if (button.id === 'equals') {
                    try {
                        display.value = eval(display.value);
                    } catch {
                        display.value = 'Error';
                    }
                } else if (button.id === 'sci-toggle') {
                    isScientific = !isScientific;
                    scientificButtons.forEach(btn => {
                        btn.style.display = isScientific ? 'block' : 'none';
                    });
                } else if (button.id === 'sqrt') {
                    display.value = Math.sqrt(parseFloat(display.value)).toFixed(4);
                } else if (button.id === 'pow') {
                    display.value += '**';
                } else if (button.id === 'log') {
                    display.value = Math.log10(parseFloat(display.value)).toFixed(4);
                } else if (button.id === 'ln') {
                    display.value = Math.log(parseFloat(display.value)).toFixed(4);
                } else if (button.id === 'pi') {
                    display.value += Math.PI;
                } else if (button.id === 'exp') {
                    display.value = Math.exp(parseFloat(display.value)).toFixed(4);
                } else if (button.id === 'factorial') {
                    let num = parseInt(display.value);
                    let result = 1;
                    for (let i = 1; i <= num; i++) {
                        result *= i;
                    }
                    display.value = result;
                } else {
                    display.value += button.innerText;
                }
            });
        });

        document.getElementById('sin').addEventListener('click', () => {
            display.value = Math.sin(parseFloat(display.value)).toFixed(4);
        });

        document.getElementById('cos').addEventListener('click', () => {
            display.value = Math.cos(parseFloat(display.value)).toFixed(4);
        });

        document.getElementById('tan').addEventListener('click', () => {
            display.value = Math.tan(parseFloat(display.value)).toFixed(4);
        });
    });

</script>

