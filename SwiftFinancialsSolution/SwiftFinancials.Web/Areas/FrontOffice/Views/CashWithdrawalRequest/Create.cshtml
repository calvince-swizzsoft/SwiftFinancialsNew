@model Application.MainBoundedContext.DTO.FrontOfficeModule.CashWithdrawalRequestDTO


@{
    ViewBag.Title = "Authorizations";
    var withdrawalNotificationCategories = ViewBag.WithdrawalNotificationCategorySelectList as List<SelectListItem>;

    var cashTransferStatus = ViewBag.CashTransferStatus as List<SelectListItem>;
    //var recordStatus = ViewBag.RecordStatus as List<SelectListItem>;

    var customerFilter = ViewBag.CustomerFilterSelectList as List<SelectListItem>;
}

<head>
    <style>
        .nav-tabs .nav-link {
            background-color: transparent;
        }

            .nav-tabs .nav-link.active {
                background-color: #f8f9fa; /* light grey */
            }

        .tab-pane {
            display: none;
        }

            .tab-pane.show {
                display: block;
            }

        .blue-section {
            margin-top: 20px;
            background-color: #e0f0ff;
            padding: 15px;
            border-radius: 8px;
        }
    </style>
</head>

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-12">
        <h2>Authorizations</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            @*<li class="breadcrumb-item">
                @Html.ActionLink("Home", "Index", "CashWithdrawalRequest", new { Area = "FrontOffice" }, null)
            </li>*@
            @*<li class="breadcrumb-item active">
                <strong>Cash Transactions</strong>
            </li>*@
        </ol>
    </div>
</div>

<div class="container-fluid mt-3">
    <div class="card">
        <div class="card-body">
            @using (Html.BeginForm("Create", "CashWithdrawalRequest", new { Area = "FrontOffice" }, FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <ul class="nav nav-tabs mb-3" id="mainTabs" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" data-toggle="tab" href="#cashDepositTab">Cash Deposit Requests</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" data-toggle="tab" href="#cashWithdrawalTab">Cash Withdrawal Requests</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" data-toggle="tab" href="#cashTransferTab">Cash Transfer Requests</a>
                    </li>
                </ul>

                <div class="tab-content" id="mainTabContent">
                    <div id="cashDepositTab" class="tab-pane fade show active">
                        <div class="card">
                            <div class="card-body">
                                <ul class="nav nav-tabs mt-3" id="cashDepositSubTabs" role="tablist">
                                    <li class="nav-item">
                                        <a class="nav-link active" data-toggle="tab" href="#depositRequestsTab">Cash Deposit Requests</a>
                                    </li>
                                    @*<li class="nav-item">
                                        <a class="nav-link" data-toggle="tab" href="#createDepositRequestTab">Create Cash Deposit Request</a>
                                    </li>*@
                                </ul>

                                <div class="tab-content mt-3" id="cashDepositSubTabContent">
                                    <div id="depositRequestsTab" class="tab-pane fade show active" role="tabpanel">
                                        <div class="table-responsive">
                                            <table id="cashDepositRequestsTable" class="table table-striped table-bordered table-hover" style="width:100%;">
                                                <tbody>
                                                    <tr>
                                                        <td class="dataTables_empty">
                                                            Loading data from server
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>

                                    <!--<div id="createDepositRequestTab" class="tab-pane fade" role="tabpanel">-->
                                        <!-- Content for Create Cash Deposit Request form -->
                                        <!--<h4>Create Cash Deposit Request</h4>-->
                                        <!-- Add your form for creating a new deposit request here -->
                                    <!--</div>-->
                                </div>
                            </div>
                        </div>
                    </div>

                    <div id="cashWithdrawalTab" class="tab-pane fade">
                        <div class="card">
                            <div class="card-body">
                                <ul class="nav nav-tabs mt-3" id="cashWithdrawalSubTabs" role="tablist">
                                    <li class="nav-item">
                                        <a class="nav-link active" data-toggle="tab" href="#withdrawalRequestsTab">Cash Withdrawal Requests</a>
                                    @*</li>
                                    <li class="nav-item">
                                        <a class="nav-link" data-toggle="tab" href="#createWithdrawalRequestTab">Create Cash Withdrawal Request</a>
                                    </li>*@
                                </ul>

                                <div class="tab-content mt-3" id="cashWithdrawalSubTabContent">
                                    <div id="withdrawalRequestsTab" class="tab-pane fade show active" role="tabpanel">
                                        <div class="table-responsive">
                                            <table id="cashWithdrawalRequestsTable" class="table table-striped table-bordered table-hover" style="width:100%;">
                                                <tbody>
                                                    <tr>
                                                        <td class="dataTables_empty">
                                                            Loading data from server
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>

                                    <!--<div id="createWithdrawalRequestTab" class="tab-pane fade" role="tabpanel">-->
                                        <!-- Content for Create Cash Withdrawal Request form -->
                                        <!--<h4>Create Cash Withdrawal Request</h4>-->
                                        <!-- Add your form for creating a new withdrawal request here -->
                                    <!--</div>-->
                                </div>
                            </div>
                        </div>
                    </div>
                    <div id="cashTransferTab" class="tab-pane fade">
                        <div class="card">
                            <div class="card-body">
                                <ul class="nav nav-tabs mt-3" id="cashTransferSubTabs" role="tablist">
                                    <li class="nav-item">
                                        <a class="nav-link active" data-toggle="tab" href="#transferRequestsTab">Cash Transfer Requests</a>
                                    </li>
                                    @*<li class="nav-item">
                                        <a class="nav-link" data-toggle="tab" href="#createTransferRequestTab">Create Cash Transfer Request</a>
                                    </li>*@
                                </ul>

                                <div class="tab-content mt-3" id="cashTransferSubTabContent">



                                    <!-- Cash Transfer Requests tab content -->
                                    <div id="transferRequestsTab" class="tab-pane fade show active" role="tabpanel">


                                        <!-- Data Table -->
                                        <table id="cashTransferRequestsTable" class="table table-striped table-bordered table-hover" style="width:100%;">
                                            <tbody>
                                                <tr>
                                                    <td class="dataTables_empty">
                                                        Loading data from server...
                                                    </td>
                                                </tr>
                                            </tbody>
                                        </table>

                                        <!-- Filter and Status Section at the Bottom -->
                                        @*<div class="row mt-3">*@
                                    <div class="blue-section">
                                        <form id="filterForm">
                                            <div class="row mb-3">
                                                <!-- Placeholder for DataTable length (items per page) -->
                                                <div class="col-lg-2">
                                                    <!-- The length dropdown will be moved here -->
                                                </div>
                                                <!-- Placeholder for pagination -->
                                                <div class="col-lg-10">
                                                    <!-- Pagination will be added here dynamically -->
                                                    <nav aria-label="Page navigation">
                                                        <ul class="pagination justify-content-end">
                                                            <!-- Pagination items will be inserted here -->
                                                        </ul>
                                                    </nav>
                                                </div>
                                            </div>
                                            <div class="row">
                                                <div class="col-lg-2">
                                                    <div class="form-group">
                                                        <label for="statusFilter">Status</label>
                                                        <div class="input-group">
                                                            <span class="input-group-addon"><i class="fa fa-bars fa-fw"></i></span>
                                                            @Html.DropDownListFor(m => m.CashTransferStatus, cashTransferStatus,
                                                            new { @class = "form-control", @id = "statusFilter" })
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-lg-2">
                                                    <div class="form-group">
                                                        <label for="customerFilter">Customer Filter</label>
                                                        <div class="input-group">
                                                            <span class="input-group-addon"><i class="fa fa-bars fa-fw"></i></span>
                                                            @Html.DropDownListFor(m => m.CustomerFilter, customerFilter,
                                                            new { @class = "form-control", @id = "customerFilter" })
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-lg-6">
                                              
                                                        <label>Search</label>
                                                    
                                                </div>
                                                <div class="col-lg-2">
                                                    <div class="form-group">
                                                        <label>&nbsp;</label> <!-- Empty label to align with other elements -->
                                                        <input type="submit" value="Refresh" id="genericSearch" class="btn btn-primary form-control" />
                                                    </div>
                                                </div>
                                            </div>
                                        </form>
                                    </div>


                                    </div>


                                    <!-- Create Cash Transfer Request form -->
                                    <!--<div id="createTransferRequestTab" class="tab-pane fade" role="tabpanel">
                                        <h4>Create Cash Transfer Request</h4>-->
                                        <!-- Add your form for creating a new transfer request here -->
                                    <!--</div>-->
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

                @*<div class="form-group">
                    <div style="margin-top: 20px; text-align: right;">
                        <button type="submit" class="btn btn-primary">Process Transaction</button>
                    </div>
                </div>*@
            }
        </div>
    </div>
</div>

<input type='hidden' id='h' name='h' />

<!-- Modal -->
<div class="modal fade" id="authorizationModal" tabindex="-1" role="dialog" aria-labelledby="authorizationModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="authorizationModalLabel">Authorize Request</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to approve or reject this request?</p>
                <div class="form-group">
                    <label for="remarks">Remarks:</label>
                    <input type="text" id="remarks" class="form-control" placeholder="Enter Text..">
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Authorize
                </button>
                <div class="dropdown-menu">
                    <a class="dropdown-item" href="#" id="authorizeBtn">Authorize</a>
                    <a class="dropdown-item" href="#" id="rejectBtn">Reject</a>
                </div>
                <button type="button" id="updateBtn" class="btn btn-success">Update</button>
            </div>
        </div>
    </div>
</div>

<div class="modal  fade" id="EmployeeLookupModal" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header  bg-primary">
                <h3 class="font-bold">Employee Lookup<button type="button" class="close" data-dismiss="modal"><span class="sr-only">Close</span></button></h3>
            </div>
            <table id="EmployeeGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                <tbody>
                    <tr>
                        <td class="dataTables_empty">
                            Loading data from server
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
<script src="~/Scripts/js/navlink.js"></script>



@section Styles{
    @Styles.Render("~/Content/plugins/iCheck/iCheckStyles")
    @Styles.Render("~/plugins/wizardStepsStyles")
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/plugins/awesomeCheckboxStyles")
    @Styles.Render("~/plugins/clockpickerStyles")
    @Styles.Render("~/plugins/dateRangeStyles")
    @Styles.Render("~/Content/plugins/chosen/chosenStyles")
    @Styles.Render("~/plugins/switcheryStyles")
    @Styles.Render("~/plugins/jasnyBootstrapStyles")
    @Styles.Render("~/plugins/nouiSliderStyles")
    @Styles.Render("~/plugins/dataPickerStyles")
    @Styles.Render("~/Content/plugins/ionRangeSlider/ionRangeStyles")
    @Styles.Render("~/plugins/imagecropperStyles")
    @Styles.Render("~/Content/plugins/colorpicker/colorpickerStyles")
    @Styles.Render("~/plugins/select2Styles")
    @Styles.Render("~/plugins/touchSpinStyles")
    @Styles.Render("~/plugins/tagInputsStyles")
    @Styles.Render("~/plugins/duallistStyles")
}


@section Scripts {

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>

    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/wizardSteps")
    @Scripts.Render("~/plugins/validate")
    @Scripts.Render("~/plugins/typehead")
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/iCheck")
    @Scripts.Render("~/plugins/dataPicker")
    @Scripts.Render("~/plugins/ionRange")
    @Scripts.Render("~/plugins/nouiSlider")
    @Scripts.Render("~/plugins/jasnyBootstrap")
    @Scripts.Render("~/plugins/switchery")
    @Scripts.Render("~/plugins/chosen")
    @Scripts.Render("~/plugins/knob")
    @Scripts.Render("~/plugins/imagecropper")
    @Scripts.Render("~/plugins/colorpicker")
    @Scripts.Render("~/plugins/clockpicker")
    @Scripts.Render("~/plugins/dateRange")
    @Scripts.Render("~/plugins/select2")
    @Scripts.Render("~/plugins/touchSpin")
    @Scripts.Render("~/plugins/tagInputs")
    @Scripts.Render("~/plugins/duallist")


    @*<script>
        $(document).ready(function () {

              $('#EmployeeGrid').DataTable({
                // Ajax settings
                "sPaginationType": "full_numbers",
                "bProcessing": true,
                "bServerSide": true,
                 "sAjaxSource": "@(Url.Action("Index", "Employee", new { Area = "HumanResource"}))",
                "sServerMethod": "POST",
                // Callback settings
                "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {

                   var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
                    var h = ("0" + createdDate.getHours()).slice(-2);
                    var m = ("0" + createdDate.getMinutes()).slice(-2);
                    var s = ("0" + createdDate.getSeconds()).slice(-2);

                    var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
                    aData.CreatedDate = dt_Created;

                    console.log("Employee ID:", aData.Id);

                    //var selectUrl = '<a  href="/FrontOffice/Teller/GetEmployeeDetailsJson/' + aData.Id + '" class = "btn btn-outline-info"">' + 'Select</a>';

                    $('td:eq(4)', nRow).html(dt_Created);

                    $('td:eq(5)', nRow).html('<button class="btn btn-outline-info select-btn" data-id="' + aData.Id + '">Select</button>');

                    return nRow;
                },
            // Display settings
            "aoColumns": [
                { "mDataProp": "Customer.FullName", "sTitle": "Name", "sWidth": "15%" },
                { "mDataProp": "BranchDescription", "sTitle": "Branch Name", "sWidth": "10%" },
                { "mDataProp": "DepartmentDescription", "sTitle": "DepartmentDescription", "sWidth": "15%" },
                { "mDataProp": "IsLocked", "sTitle": "Is Locked?", "sWidth": "15%" },
                { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "10%" },
                { "mDataProp": "CreatedDate", "sTitle": "", "sWidth": "10%", "bSortable": false }],
            "aaSorting": [[0, "desc"]]
        });

        // Use delegated event handling for dynamic elements
        $('#EmployeeGrid').on('click', '.select-btn', function () {
            var employeeId = $(this).data('id');
            console.log("Selected Employee ID:", employeeId);
            fetchEmployeeDetails(employeeId);
        });

        function fetchEmployeeDetails(employeeId) {
            console.log("Entered fetchEmployeeDetails");

            $.ajax({
                url: '/FrontOffice/Teller/GetEmployeeDetailsJson',
                type: 'GET',
                data: { employeeId: employeeId },
                success: function (data) {
                    if (data) {
                        console.log(data);
                        console.log(JSON.stringify(data));

                        // Populate the form fields with the customer details

                        console.log("emplyee name is", data.Customer.FullName)

                        $('#EmployeeId').val(data.Id);
                        $('#EmployeeCustomerFullName').val(data.Customer.FullName);



                        // Hide the lookup modal after successful data retrieval
                        $('#EmployeeLookupModal').modal('hide');
                    } else {
                        console.error("No employee details found.");
                    }
                },
                error: function (xhr, status, error) {
                    console.error("Failed to fetch employee details:", error);
                }
            });
        }

        })

    </script>*@



    <script>
        $(document).ready(function () {

            // Consolidate event handling for all tab groups
            $('body').on('click', '#mainTabs a, #cashDepositSubTabs a, #cashWithdrawalSubTabs a, #cashTransferSubTabs a', function (e) {
                e.preventDefault();
                $(this).tab('show');
            });

        })


        $('#cashDepositRequestsTable').DataTable({
    "sPaginationType": "full_numbers",
    "bProcessing": true,
    "bServerSide": true,
    "sAjaxSource": "@(Url.Action("CashDepositRequestsIndex", "CashWithdrawalRequest", new { Area = "FrontOffice"}))",
    "sServerMethod": "POST",
    "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
        var dropDownHtml = `
            <button type="button" id="cashDepositRequestBtn" class="btn btn-outline-info select-action-button" style="width: 150px;">
                Select
            </button>
        `;

        // Set the HTML for the action column
        $('td:eq(6)', nRow).html(dropDownHtml);
        return nRow;
    },
    "aoColumns": [
        { "mDataProp": "CustomerAccountCustomerFullName", "sTitle": "Customer Name", "sWidth": "15%" },
        { "mDataProp": "StatusDescription", "sTitle": "Authorization Status", "sWidth": "15%" },
        { "mDataProp": "Amount", "sTitle": "Amounts", "sWidth": "10%" },
        { "mDataProp": "CustomerAccountFullAccountNumber", "sTitle": "Account Number", "sWidth": "15%" },
        { "mDataProp": "CustomerAccountCustomerReference1", "sTitle": "Customer Reference 1", "sWidth": "10%" },
        { "mDataProp": "CustomerAccountCustomerReference2", "sTitle": "Customer Reference 2", "sWidth": "10%" },
        { "mDataProp": "PostedDate", "sTitle": "Action", "sWidth": "15%", "bSortable": false }
    ],
    "aaSorting": [[1, "desc"]]
});

// Use event delegation to handle clicks on dynamically generated buttons
$(document).on('click', '#cashDepositRequestBtn', function (e) {
    e.preventDefault();

    const rowData = $('#cashDepositRequestsTable').DataTable().row($(this).closest('tr')).data(); // Get the data for the clicked row
    const requestId = rowData.Id; // Get the ID of the selected item
    var requestType = 0;

    // Show the modal
    $('#authorizationModal').modal('show');

    // Set up the modal buttons for approval and rejection
    $('#authorizeBtn').off('click').on('click', function () {
        handleRequest(requestId, requestType, true); // Handle approve
    });

    $('#rejectBtn').off('click').on('click', function () {
        handleRequest(requestId, requestType, false); // Handle reject
    });
});

           $('#cashWithdrawalRequestsTable').DataTable({
        "sPaginationType": "full_numbers",
        "bProcessing": true,
        "bServerSide": true,
        "sAjaxSource": "@(Url.Action("CashWithdrawalRequestsIndex", "CashWithdrawalRequest", new { Area = "FrontOffice"}))",
        "sServerMethod": "POST",
        "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {


            var dropDownHtml = `
            <button type="button" id="cashWithdrawalRequestBtn", class="btn btn-outline-info select-action-button" style="width: 150px;">
                Select
            </button>
        `;

            $('td:eq(6)', nRow).html(dropDownHtml);
            return nRow;
        },
        "aoColumns": [
            { "mDataProp": "CustomerAccountCustomerFullName", "sTitle": "Customer Name", "sWidth": "15%" },
            { "mDataProp": "StatusDescription", "sTitle": "Authorization Status", "sWidth": "15%" },
            { "mDataProp": "Amount", "sTitle": "Amounts", "sWidth": "10%" },
            { "mDataProp": "CustomerAccountFullAccountNumber", "sTitle": "Account Number", "sWidth": "15%" },
            { "mDataProp": "CustomerAccountCustomerReference1", "sTitle": "Customer Refernce 1", "sWidth": "10%" },
            { "mDataProp": "CustomerAccountCustomerReference2", "sTitle": "Customer Reference 2", "sWidth": "10%" },
            { "mDataProp": "CreatedDate", "sTitle": "Action", "sWidth": "15%", "bSortable": false }
        ],
        "aaSorting": [[1, "desc"]]
           });



        $(document).on('click', '#cashWithdrawalRequestBtn', function (e) {
        e.preventDefault();

        const rowData = $('#cashWithdrawalRequestsTable').DataTable().row($(this).closest('tr')).data(); // Get the data for the clicked row
        const requestId = rowData.Id; // Get the ID of the selected item
        var requestType = 1;

        // Show the modal
        $('#authorizationModal').modal('show');

        // Set up the modal buttons for approval and rejection
        $('#authorizeBtn').off('click').on('click', function () {
            handleRequest(requestId, requestType, true); // Handle approve
        });

        $('#rejectBtn').off('click').on('click', function () {
            handleRequest(requestId, requestType, false); // Handle reject
        });
    });


    async function handleRequest(id, requestType, isApproval) {
        const remarks = $('#remarks').val();

        console.log(id);
        console.log(requestType);
        console.log(isApproval);
        console.log(remarks);

        const response = await $.ajax({
            url: '/CashWithdrawalRequest/HandleRequest',
            type: 'POST',
            data: { id, requestType, isApproval, remarks },
            success: function (result) {

                $('#authorizationModal').modal('hide');
                // Optionally, show a success message or update the UI
            },
            error: function (xhr, status, error) {
                // Handle error
                console.error(error);
            }
        });
    }
    </script>

    <script>

$(document).ready(function () {
    var table = $('#cashTransferRequestsTable').DataTable({
        "sPaginationType": "full_numbers",
        "bProcessing": true,
        "bServerSide": true,
        "sAjaxSource": "@(Url.Action("CashTransferRequestsIndex", "CashWithdrawalRequest", new { Area = "FrontOffice"}))",
        "sServerMethod": "POST",
        "fnServerParams": function (aoData) {
            // Pass selected filters as parameters
            aoData.push(
                { "name": "status", "value": $('#statusFilter').val() || 1 },
                { "name": "customerFilter", "value": $('#customerFilter').val() || 2 }
            );
        },
        "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
            var dropDownHtml = `
                <button type="button" id="cashTransferRequestBtn" class="btn btn-outline-info select-action-button" style="width: 150px;">
                    Select
                </button>
            `;
            $('td:eq(6)', nRow).html(dropDownHtml);
            return nRow;
        },
        "aoColumns": [
            { "mDataProp": "EmployeeCustomerFullName", "sTitle": "Employee Name", "sWidth": "15%" },
            { "mDataProp": "StatusDescription", "sTitle": "Authorization Status", "sWidth": "15%" },
            { "mDataProp": "Amount", "sTitle": "Amounts", "sWidth": "10%" },
            { "mDataProp": "TellerCashBalanceStatus", "sTitle": "Teller C/Balance Status", "sWidth": "15%" },
            { "mDataProp": "TellerCashBalanceStatusValue", "sTitle": "Teller C/Balance", "sWidth": "10%" },
            { "mDataProp": "CreatedBy", "sTitle": "Created By", "sWidth": "10%" },
            { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "15%", "bSortable": false }
        ],
        "aaSorting": [[1, "desc"]],
        "dom": 'lftip',  // Only the table is rendered initially (search and length will be manually added)
        "initComplete": function (settings, json) {
      
            $('#cashTransferRequestsTable_length').appendTo('.blue-section .row:first-child .col-lg-2');

            // Move DataTables search bar
            $('#cashTransferRequestsTable_filter').appendTo('.blue-section .row:last-child .col-lg-6');
        /*    $('#cashTransferRequestsTable_filter input').attr('placeholder', 'Enter Text...');*/
            $('#cashTransferRequestsTable_filter label').contents().filter(function () {
                return this.nodeType === 3; // Text nodes only
            }).remove(); // Remove the "Search:" text

            // Move pagination
            $('#cashTransferRequestsTable_paginate').appendTo('.blue-section .row:first-child .col-lg-10');
        }
    });
});



        $(document).on('click', '#cashTransferRequestBtn', function (e) {
            e.preventDefault();

            const rowData = $('#cashTransferRequestsTable').DataTable().row($(this).closest('tr')).data(); // Get the data for the clicked row
            const requestId = rowData.Id; // Get the ID of the selected item
            var requestType = 2;

            // Show the modal
            $('#authorizationModal').modal('show');

            // Set up the modal buttons for approval and rejection
            $('#authorizeBtn').off('click').on('click', function () {
                handleRequest(requestId, requestType, true); // Handle approve
            });

            $('#rejectBtn').off('click').on('click', function () {
                handleCashTransferRequest(requestId, requestType, false); // Handle reject
            });
        });


        async function handleCashTransferRequest(id, requestType, isApproval, employeeId) {
            const remarks = $('#remarks').val();

            console.log(id);
            console.log(requestType);
            console.log(isApproval);
            console.log(remarks);

            const response = await $.ajax({
                url: '/CashWithdrawalRequest/HandleTransferRequest',
                type: 'POST',
                data: { id, requestType, isApproval, remarks },
                success: function (result) {

                    $('#authorizationModal').modal('hide');
                    // Optionally, show a success message or update the UI
                },
                error: function (xhr, status, error) {
                    // Handle error
                    console.error(error);
                }
            });
        }
    </script>

}