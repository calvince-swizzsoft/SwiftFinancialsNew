@model  Application.MainBoundedContext.DTO.FrontOfficeModule.FiscalCountDTO

@{
    /**/

    ViewBag.Title = "Create CashManagement";

    var withdrawalNotificationCategories = ViewBag.WithdrawalNotificationCategorySelectList as List<SelectListItem>;

    var treasuryTransactionTypes = ViewBag.TreasuryTransactionTypeSelectList as List<SelectListItem>;


}

@using (Html.BeginForm("Search", "CashManagement", new { Area = "FrontOffice" }, FormMethod.Get))
{
    TempData["Remarks"] = "HAM";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-12">
        <h2>Cash Management</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            <li class="breadcrumb-item">
                @Html.ActionLink("Home", "Index", "CashManagement", new { Area = "FrontOffice" }, null)
            </li>
            <li class="breadcrumb-item active">
                <strong>Create</strong>
            </li>
        </ol>
    </div>
</div>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger fade in">
        <button class="close" data-dismiss="alert">
            ×
        </button>
        <i class="fa-fw fa fa-times"></i>
        <strong>Error!</strong> @TempData["Error"]
    </div>
}
<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="close-link">
                            <i class="fa fa-times"></i>
                        </a>
                    </div>
                </div>
                <div class="ibox-content bg-white text-info">
                    @using (Html.BeginForm("Create", "CashManagement", new { Area = "FrontOffice" }, FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })


                            <fieldset>

                                <div class="row">
                                    <div class="col-lg-6">

                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.TransactionType, htmlAttributes: new { @class = "" })
                                            <div class="input-group">
                                                <span class="input-group-addon"><i class="fa fa-bars fa-fw"></i></span>
                                                @Html.DropDownListFor(model => model.TransactionType, treasuryTransactionTypes, new { @id = "TransactionType", @class = "form-control input-md" })
                                                @Html.ValidationMessageFor(model => model.TransactionType)
                                            </div>
                                        </div>


                                        <div class="form-group form-group-sm">
                                            @Html.LabelFor(model => model.TotalAmount, htmlAttributes: new { @class = "control-label" })
                                            @Html.EditorFor(model => model.TotalAmount, new { htmlAttributes = new { @id = "Remark", @class = "form-control", @placeholder = "Amount", @required = "" } })
                                            @Html.ValidationMessageFor(model => model.TotalAmount, "", new { @class = "text-danger" })
                                        </div>

                                    </div>


                                    <div class="col-lg-4">

                                        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
                                        <script>
                                            $(document).ready(function () {
                                                $('#Description').blur(function () {
                                                    var description = $(this).val();
                                                    if (description.trim() === "") {
                                                        $(this).addClass('is-invalid');
                                                        $('#description-error').text('Description cannot be empty');
                                                    } else {
                                                        $(this).removeClass('is-invalid');
                                                        $('#description-error').text('');
                                                    }
                                                });
                                            });
                                        </script>

                                        <div class="form-group form-group-sm">
                                            @Html.LabelFor(model => model.Description, htmlAttributes: new { @class = "control-label" })
                                            @Html.EditorFor(model => model.Description, new { htmlAttributes = new { @id = "Description", @class = "form-control", @placeholder = "Description", disabled = "disabled" } })
                                            @Html.HiddenFor(model => model.Description)
                                            @Html.HiddenFor(model => model.PostingPeriodId)
                                            @Html.HiddenFor(model => model.BranchDescription)
                                            @Html.HiddenFor(model => model.TellerDescription)
                                            @Html.HiddenFor(model => model.TreasuryDescription)
                                            @Html.HiddenFor(model => model.TellerId)
                                            @Html.HiddenFor(model => model.TreasuryId)
                                            @Html.HiddenFor(model => model.BranchId)
                                            <div id="description-error" class="text-danger"></div>
                                        </div>


                                        <div class="form-group form-group-sm">
                                            @Html.LabelFor(model => model.Reference, htmlAttributes: new { @class = "control-label" })
                                            @Html.EditorFor(model => model.Reference, new { htmlAttributes = new { @id = "Remark", @class = "form-control", @placeholder = "Remarks", @required = "" } })
                                            @Html.ValidationMessageFor(model => model.Reference, "", new { @class = "text-danger" })
                                        </div>

                                    </div>
                                </div>

                                <div class="row">
                                    <!-- Denominations form starts from here -->
                                    <meta charset="UTF-8">
                                    <meta name="viewport" content="width=device-width, initial-scale=1.0">
                                    <style>
                                        label {
                                            display: block;
                                            margin-bottom: 8px;
                                        }

                                        #denominationForm {
                                            display: list-item; /* Hide the form by default */
                                        }
                                    </style>

                                    <script>
                                        // Define denominations and their values
                                        const denominations = {
                                            "Thousands": 1000,
                                            "Five Hundreds": 500,
                                            "Two Hundreds": 200,
                                            "Hundreds": 100,
                                            "20 Shillings": 20,
                                            "10 Shillings": 10,
                                            "5 Shillings": 5,
                                            "1 Shilling": 1,
                                            "50 Cents": 0.50
                                        };

                                        function toggleForm() {
                                            var form = document.getElementById('denominationForm');
                                            form.style.display = form.style.display === 'none' ? 'block' : 'none';
                                        }

                                        function calculateTotal() {
                                            var total = 0;

                                            // Calculate total based on denominations
                                            for (const denomination in denominations) {
                                                const value = denominations[denomination];
                                                const input = document.getElementById(denomination.replace(/\s+/g, '_').toLowerCase());
                                                const amount = parseInt(input.value) || 0;
                                                total += amount * value;
                                            }

                                            // Update breakdown
                                            document.getElementById('totalBreakdown').innerHTML = "Total: " + total + " KES";
                                        }
                                    </script>

                                    <button onclick="toggleForm()">Denominations Form</button>
                                    <div class="col-lg-6" id="denominationForm">
                                        <h2>Enter Amount in Kenyan Shillings</h2>

                                        <form action="#" method="post">
                                            <!-- Your form content here, dynamically generated based on denominations -->
                                            <script>
                                                // Generate input fields dynamically based on denominations
                                                for (const denomination in denominations) {
                                                    document.write('<label for="' + denomination.replace(/\s+/g, '_').toLowerCase() + '">' + denomination + ':</label>');
                                                    document.write('<input type="number" id="' + denomination.replace(/\s+/g, '_').toLowerCase() + '" name="' + denomination.replace(/\s+/g, '_').toLowerCase() + '" min="0" step="1" oninput="calculateTotal()">');
                                                }
                                            </script>

                                            <h3>Total Currency</h3>
                                            <p id="totalBreakdown">Total: 0 KES</p>
                                        </form>
                                    </div>
                                    <!-- The lookup ends here -->
                                </div>




                                <div class="row">

                                    <div class="form-group form-group-sm">
                                        <div class="col-md-offset-2">
                                            <input type="submit" value="Create" class="btn btn-primary" />
                                        </div>


                                    </div>

                                </div>


                            </fieldset>

                        </div>
                    }
                </div>

            </div>

        </div>
    </div>
</div>













<div class="modal  fade" id="customerLookupModal" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header  bg-primary">
                <h3 class="font-bold">Customers Lookup<button type="button" class="close" data-dismiss="modal"><span class="sr-only">Close</span></button></h3>
            </div>
            <table id="customersGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                <tbody>
                    <tr>
                        <td class="dataTables_empty">
                            Loading data from server
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>







<div class="modal  fade" id="EmployeeLookupModal" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header  bg-primary">
                <h3 class="font-bold">Tellers Lookup<button type="button" class="close" data-dismiss="modal"><span class="sr-only">Close</span></button></h3>
            </div>
            <table id="EmployeesGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                <tbody>
                    <tr>
                        <td class="dataTables_empty">
                            Loading data from server
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>






<div class="modal  fade" id="TresuriesLookupModal" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header  bg-primary">
                <h3 class="font-bold">Treasuries Lookup<button type="button" class="close" data-dismiss="modal"><span class="sr-only">Close</span></button></h3>
            </div>
            <table id="TresuriessGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                <tbody>
                    <tr>
                        <td class="dataTables_empty">
                            Loading data from server
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>





<div class="modal  fade" id="BankssLookupModal" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header  bg-primary">
                <h3 class="font-bold">Banks Lookup<button type="button" class="close" data-dismiss="modal"><span class="sr-only">Close</span></button></h3>
            </div>
            <table id="BankssGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                <tbody>
                    <tr>
                        <td class="dataTables_empty">
                            Loading data from server
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>






<input type='hidden' id='h' name='h' />

@section Styles{
    @Styles.Render("~/Content/plugins/iCheck/iCheckStyles")
    @Styles.Render("~/plugins/wizardStepsStyles")
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/plugins/awesomeCheckboxStyles")
    @Styles.Render("~/plugins/clockpickerStyles")
    @Styles.Render("~/plugins/dateRangeStyles")
    @Styles.Render("~/Content/plugins/chosen/chosenStyles")
    @Styles.Render("~/plugins/switcheryStyles")
    @Styles.Render("~/plugins/jasnyBootstrapStyles")
    @Styles.Render("~/plugins/nouiSliderStyles")
    @Styles.Render("~/plugins/dataPickerStyles")
    @Styles.Render("~/Content/plugins/ionRangeSlider/ionRangeStyles")
    @Styles.Render("~/plugins/imagecropperStyles")
    @Styles.Render("~/Content/plugins/colorpicker/colorpickerStyles")
    @Styles.Render("~/plugins/select2Styles")
    @Styles.Render("~/plugins/touchSpinStyles")
    @Styles.Render("~/plugins/tagInputsStyles")
    @Styles.Render("~/plugins/duallistStyles")
}

@section Scripts{
    @Scripts.Render("~/plugins/wizardSteps")
    @Scripts.Render("~/plugins/validate")
    @Scripts.Render("~/plugins/typehead")
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/iCheck")
    @Scripts.Render("~/plugins/dataPicker")
    @Scripts.Render("~/plugins/ionRange")
    @Scripts.Render("~/plugins/nouiSlider")
    @Scripts.Render("~/plugins/jasnyBootstrap")
    @Scripts.Render("~/plugins/switchery")
    @Scripts.Render("~/plugins/chosen")
    @Scripts.Render("~/plugins/knob")
    @Scripts.Render("~/plugins/imagecropper")
    @Scripts.Render("~/plugins/colorpicker")
    @Scripts.Render("~/plugins/clockpicker")
    @Scripts.Render("~/plugins/dateRange")
    @Scripts.Render("~/plugins/select2")
    @Scripts.Render("~/plugins/touchSpin")
    @Scripts.Render("~/plugins/tagInputs")
    @Scripts.Render("~/plugins/duallist")

<script>
        $(document).ready(function () {
            var selectedValue = localStorage.getItem('selectedTransactionType'); // Get selected value from local storage
            var modalShown = false; // Flag to track if modal has been shown for the selected value

            // Function to show the appropriate modal based on the selected value without delay
            function showCorrectModal() {
                // Hide all modals initially
                $('#BankssLookupModal, #EmployeeLookupModal, #TresuriesLookupModal').modal('hide');

                // Show the modal based on the selected value if it hasn't been shown before
                if (!modalShown) {
                    switch (selectedValue) {
                        case '1':
                            $('#TresuriesLookupModal').modal('show'); // Show modal
                            setTimeout(function () {
                                $('#BankssLookupModal, #EmployeeLookupModal, #TresuriesLookupModal').modal('hide');
                            }, 10); // 10 minutes timeout
                            break;
                        case '2':
                        case '4':
                            $('#BankssLookupModal').modal('show'); // Show modal
                            setTimeout(function () {
                                $('#BankssLookupModal, #EmployeeLookupModal, #TresuriesLookupModal').modal('hide');
                            }, 10); // 10 minutes timeout
                            break;
                        case '8':
                            $('#EmployeeLookupModal').modal('show'); // Show modal
                            setTimeout(function () {
                                $('#BankssLookupModal, #EmployeeLookupModal, #TresuriesLookupModal').modal('hide');
                            }, 10); // 10 minutes timeout
                            break;
                        default:
                            // Handle default case if necessary
                            break;
                    }
                    modalShown = true; // Set flag to true indicating modal has been shown
                }
            }

            // Handle dropdown change event
            $('#TransactionType').change(function () {
                selectedValue = $(this).val();
                localStorage.setItem('selectedTransactionType', selectedValue); // Save selected value to local storage
                modalShown = false; // Reset flag when dropdown value changes
                showCorrectModal();
            });

            // Initialize dropdown with persisted value
            if (selectedValue) {
                $('#TransactionType').val(selectedValue).change();
            }
        });









    //ChartOfAccount typehead starts here
        $('.EntriesChartOfAccountsLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetChartOfAccountsAsync", "ChartOfAccount", new { Area = "Accounts" })',
                        { query: query },
                        function (ChartOfAccounts) {
                            var newChartOfAccountsData = [];
                            ChartOfAccountsMap = {};
                            $.each(ChartOfAccounts, function (i, ChartOfAccount) {
                                newChartOfAccountsData.push(ChartOfAccount.AccountName);
                                ChartOfAccountsMap[ChartOfAccount.AccountName] = ChartOfAccount;
                            });
                            process(newChartOfAccountsData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var chartOfAccountId = "";
                    ChartOfAccountId = ChartOfAccountsMap[item].Id;

                    $('#ChartOfAccountId').val(ChartOfAccountId);

                    //console.log(ChartOfAccountId)
                    return item;
                }
            });

         //Branch typehead starts here
         $('.BranchesLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetBranchesAsync", "Branch", new { Area = "Admin" })',
                        { query: query },
                        function (Branches) {
                            var newBranchesData = [];
                            BranchesMap = {};
                            $.each(Branches, function (i, Branch) {
                                newBranchesData.push(Branch.Description);
                                BranchesMap[Branch.Description] = Branch;
                            });
                            process(newBranchesData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var branchId = "";
                    BranchId  = BranchesMap[item].Id;
                    $('#BranchId').val(BranchId);
                    $('#BranchDescription').val(Description);
                    //console.log(BranchId)
                    return item;
                }
            });


          //PostingPeriod typehead starts here
            $('.PostingPeriodsLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetPostingPeriodsAsync", "PostingPeriod", new { Area = "Accounts" })',
                        { query: query },
                        function (PostingPeriods) {
                            var newPostingPeriodsData = [];
                            PostingPeriodsMap = {};
                            $.each(PostingPeriods, function (i, PostingPeriod) {
                                newPostingPeriodsData.push(PostingPeriod.Description);
                                PostingPeriodsMap[PostingPeriod.Description] = PostingPeriod;
                            });
                            process(newPostingPeriodsData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var postingPeriodId = "";
                    PostingPeriodId = PostingPeriodsMap[item].Id;
                    $('#PostingPeriodId').val(PostingPeriodId);
                    //console.log(PostingPeriodId)
                    return item;
                }
            });


        $(document).ready(function () {
            $('#data_3 .input-group.date').datepicker({
                startView: 2,
                todayBtn: "linked",
                keyboardNavigation: false,
                forceParse: false,
                autoclose: true,
            });
        });
             //Station typehead starts here

            $('.LeaveTypeLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetLeaveTypesAsync", "LeaveType", new { Area = "HumanResource" })',
                        { query: query },
                        function (LeaveTypes) {
                            var newLeaveTypesData = [];
                            LeaveTypesMap = {};
                            $.each(LeaveTypes, function (i, LeaveType) {
                                newLeaveTypesData.push(LeaveType.Description);
                                LeaveTypesMap[LeaveType.Description] = LeaveType;
                            });
                            process(newLeaveTypesData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var LeaveTypeId = "";
                    LeaveTypeId = LeaveTypesMap[item].Id;
                    $('#LeaveTypeId').val(LeaveTypeId);
                    $('#LeaveTypeZoneDivisionEmployerDescription').val(LeaveTypesMap[item].ZoneDivisionEmployerDescription);
                    $('#LeaveTypeZoneDescription').val(LeaveTypesMap[item].ZoneDescription);
                    $('#LeaveTypeZoneDivisionDescription').val(LeaveTypesMap[item].ZoneDivisionDescription);
                    //console.log(LeaveTypeId)
                    return item;
                }
            });
$('#EmployeesGrid').DataTable({
    // Ajax settings
    "sPaginationType": "full_numbers",
    "bProcessing": true,
    "bServerSide": true,
    "sAjaxSource": "@(Url.Action("Index", "Teller", new { Area = "FrontOffice" }))",
    "sServerMethod": "POST",
    // Callback settings
    "fnRowCallback": function(nRow, aData, iDisplayIndex, iDisplayIndexFull) {
        var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
        var h = ("0" + createdDate.getHours()).slice(-2);
        var m = ("0" + createdDate.getMinutes()).slice(-2);
        var s = ("0" + createdDate.getSeconds()).slice(-2);
        var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
        aData.CreatedDate = dt_Created;

        var selectUrl = '<button data-id="' + aData.Id + '" data-full-name="' + aData.Description + '" class="btn btn-outline-info select-btn">Select</button>';

        $('td:eq(4)', nRow).html(dt_Created);
        $('td:eq(5)', nRow).html(selectUrl);

        return nRow;
    },
    // Display settings
    "aoColumns": [
        { "mDataProp": "Description", "sTitle": "Name", "sWidth": "15%" },
        { "mDataProp": "TypeDescription", "sTitle": "Type", "sWidth": "10%" },
        { "mDataProp": "Reference", "sTitle": "Reference", "sWidth": "10%" },
        { "mDataProp": "ChartOfAccountAccountName", "sTitle": "ChartOfAccountAccountName", "sWidth": "15%" },
        { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "10%" },
        { "mDataProp": null, "sTitle": "", "sWidth": "10%", "bSortable": false }
    ],
    "aaSorting": [[0, "desc"]]
});

// Event delegation for dynamically added select buttons
$("#EmployeesGrid tbody").on("click", ".select-btn", function() {
    var id = $(this).data('id');
    var fullName = $(this).data('full-name');

    // Set values based on selected data
    $('#TellerId').val(id);
    $('input[name=EmployeeCustomerId]').val(id);
    $('#Description').val(fullName); // Set the selected full name to the Description textbox

    // Close the modal
    $('#EmployeeLookupModal').modal('hide');
});













    $(document).ready(function () {
        $('#data_3 .input-group.date').datepicker({
            startView: 2,
            todayBtn: "linked",
            keyboardNavigation: false,
            forceParse: false,
            autoclose: true,
        });
    });
         //Branch typehead starts here
    $('.LeaveTypeLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetLeaveTypesAsync", "LeaveType", new { Area = "HumanResource" })',
                        { query: query },
                        function (Leavetypes) {
                            var newLeavetypesData = [];
                            LeavetypesMap = {};
                            $.each(Leavetypes, function (i, Leavetype) {
                                newLeavetypesData.push(Leavetype.Description);
                                LeavetypesMap[Leavetype.Description] = Leavetype;
                            });
                            process(newLeavetypesData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var LeaveTypeId = "";
                    LeaveTypeId = LeaveTypeMap[item].Id;
                    $('#LeaveTypeId').val(LeaveTypeId);
                    //console.log(BranchId)
                    return item;
                }
            });








$('#TresuriessGrid').DataTable({
    // Ajax settings
    "sPaginationType": "full_numbers",
    "bProcessing": true,
    "bServerSide": true,
    "sAjaxSource": "@(Url.Action("Index", "Treasury", new { Area = "FrontOffice" }))",
    "sServerMethod": "POST",
    // Callback settings
    "fnRowCallback": function(nRow, aData, iDisplayIndex, iDisplayIndexFull) {
        var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
        var h = ("0" + createdDate.getHours()).slice(-2);
        var m = ("0" + createdDate.getMinutes()).slice(-2);
        var s = ("0" + createdDate.getSeconds()).slice(-2);
        var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
        aData.CreatedDate = dt_Created;

        var selectUrl = '<button data-id="' + aData.Id + '" data-description="' + aData.Description + '" class="btn btn-outline-info select-btn">Select</button>';

        $('td:eq(3)', nRow).html(dt_Created);
        $('td:eq(4)', nRow).html(selectUrl);

        return nRow;
    },
    // Display settings
    "aoColumns": [
        { "mDataProp": "Description", "sTitle": "Treasury Name", "sWidth": "15%" },
        { "mDataProp": "ChartOfAccountAccountName", "sTitle": "COA Name", "sWidth": "15%" },
        { "mDataProp": "BranchDescription", "sTitle": "Branch Name", "sWidth": "15%" },
        { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "10%" },
        { "mDataProp": null, "sTitle": "", "sWidth": "10%", "bSortable": false }
    ],
    "aaSorting": [[0, "desc"]]
});

$("#TresuriessGrid tbody").on("click", ".select-btn", function() {
    var id = $(this).data('id');
    var description = $(this).data('description');

    // Set values based on selected data
    $('input[name=TreasuryId]').val(id);
    $('input[name=Description]').val(description);

    // Close the modal permanently
    $('#TresuriesLookupModal').modal('hide');
});

// Add click event for lookup buttons to mimic the behavior of select button
$("#TresuriessGrid tbody").on("click", ".lookup-btn", function() {
    var id = $(this).data('id');
    var description = $(this).data('description');

    // Set values based on selected data
    $('input[name=TreasuryId]').val(id);
    $('input[name=Description]').val(description);

    // Close the modal permanently
    $('#TresuriesLookupModal').modal('hide');
});




$('#BankssGrid').DataTable({
    // Ajax settings
    "sPaginationType": "full_numbers",
    "bProcessing": true,
    "bServerSide": true,
    "sAjaxSource": "@(Url.Action("Index", "Bank", new { Area = "Admin" }))",
    "sServerMethod": "POST",
    // Callback settings
    "fnRowCallback": function(nRow, aData, iDisplayIndex, iDisplayIndexFull) {
        var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
        var h = ("0" + createdDate.getHours()).slice(-2);
        var m = ("0" + createdDate.getMinutes()).slice(-2);
        var s = ("0" + createdDate.getSeconds()).slice(-2);
        var dt_Created = ("0" + createdDate.getDate()).slice(-2) + "/" + ("0" + (createdDate.getMonth() + 1)).slice(-2) + "/" + createdDate.getFullYear() + " " + h + ":" + m + ":" + s;
        aData.CreatedDate = dt_Created;

        var selectUrl = '<button data-id="' + aData.Id + '" data-description="' + aData.Description + '" class="btn btn-outline-info select-btn">Select</button>';

        $('td:eq(2)', nRow).html(dt_Created);
        $('td:eq(3)', nRow).html(selectUrl);

        return nRow;
    },
    // Display settings
    "aoColumns": [
        { "mDataProp": "Description", "sTitle": "Bank", "sWidth": "15%" },
        { "mDataProp": "Code", "sTitle": "Code", "sWidth": "10%" },
        { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "10%" },
        { "mDataProp": null, "sTitle": "", "sWidth": "10%", "bSortable": false }
    ],
    "aaSorting": [[0, "desc"]]
});

$("#BankssGrid tbody").on("click", ".select-btn", function() {
    var id = $(this).data('id');
    var description = $(this).data('description');

    // Set values based on selected data
    $('#Id').val(id);
    $('input[name=BranchId]').val(id);
    $('#Description').val(description);

    // Close the modal
    $('#BankssLookupModal').modal('hide');
});

// Behavior for lookup buttons
$(".lookup-btn").click(function() {
    var id = $(this).data('id');
    var description = $(this).data('description');

    // Set values based on selected data
    $('#Id').val(id);
    $('input[name=BranchId]').val(id);
    $('#Description').val(description);

    // Close the modal
    $('#BankssLookupModal').modal('hide');
});







</script>
}


