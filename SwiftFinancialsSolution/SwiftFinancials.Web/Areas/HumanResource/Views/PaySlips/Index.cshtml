
@{
    ViewBag.Title = "SalaryPeriod PaySlips";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>SalaryPeriods PaySlips</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            <li class="breadcrumb-item active">
                <strong>List</strong>
            </li>
        </ol>
    </div>
</div>

<div class="col-lg-12">
    <div class="nav1">
        <div class="col-lg-12">
            <nav class="nav">
                <a href="@Url.Action("Index", "Salary", new { Area = "HumanResource" })" class="nav-link">

                    <i class="fa fa-cog"></i> Salary Heads
                </a>
                <a href="@Url.Action("Index", "SalaryGroups", new { Area = "HumanResource" })" class="nav-link">
                    <i class="fa fa-cog"></i> Salary Groups
                </a>
                <a href="@Url.Action("Index", "SalaryCards", new { Area = "HumanResource" })" class="nav-link">
                    <i class="fa fa-cog"></i> Salary Cards
                </a>
                <a href="@Url.Action("Index", "SalaryPeriods", new { Area = "HumanResource" })" class="nav-link">
                    <i class="fa fa-cog"></i> Salary Periods
                </a>
                <a href="@Url.Action("Index", "SalaryProcessing", new { Area = "HumanResource" })" class="nav-link">
                    <i class="fa fa-cog"></i> Salary Processing
                </a>
                <a href="@Url.Action("Index", "PaySlips", new { Area = "HumanResource" })" class="nav-link">
                    <i class="fa fa-cog"></i> Pay Slips
                </a>
                <a href="@Url.Action("Index", "PeriodClosings", new { Area = "HumanResource" })" class="nav-link">
                    <i class="fa fa-cog"></i> Close Salary Period
                </a>




            </nav>

        </div>

    </div>
</div>
<style>



    .col-lg-2 {
        display: flex;
        align-items: center; /* Center the title vertically */
        justify-content: center; /* Center the title horizontally if needed */
        padding: 0; /* Remove default padding if necessary */
        border-radius: 4px; /* Rounded corners */
        @*border: 1px solid black;*@ /* Border color and style */
    }
</style>

<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    @Html.ActionLink("Create Salary Period", "Create", "SalariPeriods", new { Area = "HumanResource" }, new { @class = "btn btn-primary" })
                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="close-link">
                            <i class="fa fa-times"></i>
                        </a>
                    </div>
                </div>
                <div class="ibox-content">
                    <div class="sk-spinner sk-spinner-double-bounce">
                        <div class="sk-double-bounce1"></div>
                        <div class="sk-double-bounce2"></div>
                    </div>
                    <div class="table-responsive">
                        <table id="salaryPeriodsGrid" class="table table-striped table-bordered table-hover" width="100%">
                            <tbody>
                                <tr>
                                    <td colspan="7" class="dataTables_empty">
                                        Loading data from server
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}

@section scripts {
    @Scripts.Render("~/plugins/dataTables")

    <script type="text/javascript">

        $(document).ready(function () {
            $('#salaryPeriodsGrid').DataTable({
                // Ajax settings
                "sPaginationType": "full_numbers",
                "bProcessing": true,
                "bServerSide": true,
                "sAjaxSource": "@(Url.Action("Index", "PaySlips", new { Area = "HumanResource" }))",
                "sServerMethod": "POST",
                // Callback settings
                "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {

                   var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
                    var h = ("0" + createdDate.getHours()).slice(-2);
                    var m = ("0" + createdDate.getMinutes()).slice(-2);
                    var s = ("0" + createdDate.getSeconds()).slice(-2);

                    var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
                    aData.CreatedDate = dt_Created;

                    var dropdownHtml = `
                        <div class="dropdown">
                            <button style="width: 150px;" class="btn btn-outline-info dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Action
                            </button>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">`;


                    var viewButton = `<a class="dropdown-item" href="/HumanResource/Payslips/details/${aData.Id}">PaySlip</a>`;



                    $('td:eq(7)', nRow).html(dt_Created);


                    switch (aData.Status) {
                        case 1: // Pending
                            dropdownHtml += viewButton;
                            break;
                        default:
                            dropdownHtml += viewButton;
                            break;
                    }

                    dropdownHtml += `</div></div>`;



                    $('td:eq(7)', nRow).html(dt_Created);
                    $('td:eq(8)', nRow).html(dropdownHtml);

                    return nRow;
                },
                // Display settings
                "aoColumns": [
                    { "mDataProp": "PostingPeriodDescription", "sTitle": "Posting Period", "sWidth": "15%" },
                    { "mDataProp": "StatusDescription", "sTitle": "Status", "sWidth": "8%" },
                    { "mDataProp": "EmployeeCategoryDescription", "sTitle": "Employee Type", "sWidth": "10%" },
                    { "mDataProp": "TaxReliefAmount", "sTitle": "Tax Relief", "sWidth": "8%" },
                    { "mDataProp": "MaximumProvidentFundReliefAmount", "sTitle": "Maximum Provident Relief Amount", "sWidth": "10%" },
                    { "mDataProp": "MaximumInsuranceReliefAmount", "sTitle": "Maximum Insurance Relief", "sWidth": "18%" },
                    { "mDataProp": "Remarks", "sTitle": "Remarks", "sWidth": "5%" },
                    { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "5%" },
                    { "mDataProp": "CreatedDate", "sTitle": "Action", "sWidth": "10%", "bSortable": false }],
                "aaSorting": [[0, "desc"]],
        });
        });

        $(document).ajaxStart(function () {
            $('.ibox').children('.ibox-content').toggleClass('sk-loading');
        });
        $(document).ajaxStop(function () {
            $('.ibox').children('.ibox-content').toggleClass('sk-loading');
        });
    </script>

}
