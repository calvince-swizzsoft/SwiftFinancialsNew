@*@model IEnumerable<SwiftFinancials.Web.Areas.Reports.Models.DynamicReport>

@{
    ViewBag.Title = "Reports";
}

<style>
    .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
        padding: 20px;
        box-sizing: border-box;
    }

    .search-container {
        width: 100%;
        margin: 20px 0 10px;
        position: relative;
        padding: 20px;
        border-radius: 20px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        backdrop-filter: blur(5px);
    }

    #searchBox {
        width: 100%;
        padding: 15px 20px;
        font-size: 16px;
        border: none;
        border-radius: 30px;
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease-in-out;
        outline: none;
        background: #fff;
    }

        #searchBox:focus {
            box-shadow: 0 12px 20px rgba(0, 0, 0, 0.2);
            transform: translateY(-2px);
        }

    #itemsList {
        display: grid;
        grid-template-columns: repeat(6, 1fr);
        gap: 20px;
        padding: 20px;
        margin: 0;
        width: 100%;
        max-height: 600px;
        overflow-y: auto;
        border-radius: 20px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        backdrop-filter: blur(5px);
    }

    .item {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        padding: 20px;
        background: #ffffff;
        border-radius: 15px;
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease;
        transform-style: preserve-3d;
        perspective: 1000px;
        transform: rotateY(0deg);
        height: 150px;
        width: 150px;
        font-weight: bold;
        text-align: center;
    }

        .item:hover {
            transform: translateY(-5px) scale(1.02) rotateY(5deg);
            box-shadow: 0 12px 25px rgba(0, 0, 0, 0.2);
            background: linear-gradient(135deg, #ffffff, #f0f0f0);
        }

        .item button {
            background: linear-gradient(135deg, #4a90e2, #357ae8);
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            color: #fff;
            cursor: pointer;
            font-size: 12px;
            transition: background 0.3s ease, transform 0.3s ease;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 5px;
        }

            .item button:hover {
                background: linear-gradient(135deg, #357ae8, #4a90e2);
                transform: scale(1.05);
                box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
            }

    

    .popup {
        display: none; 
        position: fixed; 
        z-index: 1000;
        left: 0;
        top: 0;
        width: 100%; 
        height: 100%; 
        background-color: rgba(0, 0, 0, 0.5); 
    }

    .popup-content {
        background-color: #fefefe;
        margin: 15% auto; 
        padding: 20px;
        border: 1px solid #888;
        width: 30%; 
        text-align: center;
        border-radius: 10px;
    }

    .close {
        color: red;
        float: right;
        font-size: 28px;
        font-weight: bold;
        cursor: pointer;
    }

        .close:hover,
        .close:focus {
            color: darkred;
            text-decoration: none;
            cursor: pointer;
        }

    .emoji {
        color: red;
        font-size: 48px;
        font-weight: bold;
        perspective: 1000px;
    }
</style>

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h1>Administration</h1>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            <li class="breadcrumb-item active">
                <strong>List</strong>
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h2>Reports</h2>
                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="close-link">
                            <i class="fa fa-times"></i>
                        </a>
                    </div>
                </div>
                <div class="ibox-content">
                    <div class="search-container">
                        <input type="text" id="searchBox" placeholder="Search Report..." onkeyup="filterItems()" />
                    </div>

                    <div class="container">
                        <p id="noResultsMessage" style="display: none; color: red; font-weight: bold; font-size: medium;">Search term not found !</p>

                        <ul id="itemsList">
                            @foreach (var report in Model)
                            {
                                <li class="item" data-name="@report.StoredProcedureName.ToLower()">
                                    <div style="font-weight: bold;">@report.Name</div>
                                    <div class="item-buttons">
                                        <a href="/Reports/DynamicReporting/Excel?StoredProcedureName=@report.StoredProcedureName" style="margin-right: 12px; color: green;" class="excel-link">
                                            <i class="fa fa-table" aria-hidden="true"></i> EXCEL
                                        </a>
                                        <a href="/Reports/DynamicReporting/PDF?StoredProcedureName=@report.StoredProcedureName" style="color: red;" class="pdf-link">
                                            <i class="fa fa-file-pdf-o" aria-hidden="true"></i> PDF
                                        </a>
                                    </div>
                                </li>
                            }
                        </ul>
                    </div>
                </div>
                <div class="marquee-container">
                    <div class="marquee-text">
                        The above grid is a list of all reports. Use the Search Bar to find a specific report. Available actions include <em style="color: green;">Excel</em> and <em style="color: red;">PDF</em> reports.
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


<link href="~/Scripts/css/Marquee.css" rel="stylesheet" />
<script>
    document.addEventListener('DOMContentLoaded', function () {
        function filterItems() {
            const searchTerm = document.getElementById('searchBox').value.toLowerCase();
            const items = document.querySelectorAll('#itemsList .item');
            let itemFound = false;

            items.forEach(item => {
                const itemName = item.getAttribute('data-name').toLowerCase();
                if (itemName.includes(searchTerm)) {
                    item.style.display = '';
                    itemFound = true;
                } else {
                    item.style.display = 'none';
                }
            });

            const noResultsMessage = document.getElementById('noResultsMessage');
            if (!itemFound) {
                noResultsMessage.style.display = 'block';
            } else {
                noResultsMessage.style.display = 'none';
            }
        }

        function showPopup() {
            const popup = document.getElementById('popup');
            popup.style.display = 'block';
        }

        function sendRequest(url, event) {
            event.preventDefault();  // Prevent the default anchor behavior
            fetch(url, {
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                },
            })
                .then(response => response.json())
                .then(data => {
                    console.log('Success:', data);
                })
                .catch(error => {
                    console.error('Error:', error);
                });
        }

        document.getElementById('searchBox').addEventListener('keyup', filterItems);
    });
</script>*@
