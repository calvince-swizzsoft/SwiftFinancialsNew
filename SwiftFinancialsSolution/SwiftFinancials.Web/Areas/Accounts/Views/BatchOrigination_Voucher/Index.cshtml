@{
    ViewBag.Title = "Journal Voucher";
}

<style>

    .nav {
        display: flex;
        flex-wrap: wrap; /* Allows the links to wrap onto multiple lines */
        padding: 0;
        margin: 5px;
        margin-bottom:0;
        border: 2px solid Black;
        border-radius:8px;
        margin-top:20px;
        gap: 10px; /* Space between links */
        text-align: center; /* Centers the text inside links */

        padding:10px;
    }

    .nav-link {
        display: flex;


        text-decoration: none;
        color: #007bff; /* Default text color */
        transition: color 0.3s ease, background-color 0.3s ease; /* Smooth transition */

        border-radius: 4px; /* Rounded corners */
        font-size: 14px; /* Adjust font size for better fit */
        white-space: nowrap; /* Prevents text from wrapping inside the link */
        
       @* border: 1px solid #007bff;*@ /* Border color and style */
    }

        .nav-link:hover {
            color: #fff; /* Text color on hover */
            background-color: #0056b3; /* Background color on hover */
        }

        .nav-link i {
            margin-right: 8px; /* Space between icon and text */
            font-size: 16px; /* Adjust icon size */
        }

        .nav-link.disabled {
            pointer-events: none;
            opacity: 0.6;
            cursor: not-allowed;
        }

    .col-lg-2 {
        display: flex;
        align-items: center; /* Center the title vertically */
        justify-content: center; /* Center the title horizontally if needed */
        padding: 0; /* Remove default padding if necessary */
        border-radius: 4px; /* Rounded corners */
        @*border: 1px solid black;*@ /* Border color and style */
    }
</style>

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>Accounts</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            <li class="breadcrumb-item active">
                <strong>List</strong>
            </li>
        </ol>
    </div>
</div>

<div class="row row-center">
    
    <div class="col-lg-12">
        <div class="nav1">
            <div class="col-lg-12">
                <nav class="nav">
                    <a href="@Url.Action("Index", "BatchOrigination_Voucher", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Voucher
                    </a>
                    <a href="@Url.Action("Index", "BatchOrigination_Credit", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Credit Batch
                    </a>
                    <a href="@Url.Action("Index", "BatchOrigination_Debit", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Debit Batch
                    </a>
                    <a href="@Url.Action("Index", "BatchOrigination_Refund", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Refund Batch
                    </a>
                    <a href="@Url.Action("Index", "BatchOrigination_Disbursement", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Disbursement Batch
                    </a>
                    <a href="@Url.Action("Index", "BatchOrigination_WireTransfer", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Wire Transfer Batch
                    </a>
                    <a href="@Url.Action("Index", "AddGeneralLedger", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> General Ledger Batch
                    </a>
                    <a href="@Url.Action("Index", "BatchOrigination_Reversal", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Reversal Batch
                    </a>
                    <a href="@Url.Action("Index", "InterAccountTransfer", new { Area = "Accounts" })" class="nav-link">
                        <i class="fa fa-cog"></i> Inter Account Transfer Batch
                    </a>

                </nav>
            </div>

        </div>
    </div>
</div>

<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">

                <div class="ibox-title">
                    @Html.ActionLink("Create Journal Voucher", "Create", "BatchOrigination_Voucher", new { Area = "Accounts" }, new { @class = "btn btn-primary" })

                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success">
                            <strong>Success !</strong> @TempData["SuccessMessage"]

                            <button type="button" class="close" data-dismiss="alert">
                                <span>
                                    &times;
                                </span>
                            </button>
                        </div>
                    }
                    @if (TempData["verifySuccess"] != null)
                    {
                        <div class="alert alert-success">
                            <strong>Success !</strong> @TempData["Success"]

                            <button type="button" class="close" data-dismiss="alert">
                                <span>
                                    &times;
                                </span>
                            </button>
                        </div>
                    }

                    @if (TempData["AuthSucess"] != null)
                    {
                        <div class="alert alert-success">
                            <strong>Success !</strong> @TempData["AuthSucess"]

                            <button type="button" class="close" data-dismiss="alert">
                                <span>
                                    &times;
                                </span>
                            </button>
                        </div>
                    }

                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="close-link">
                            <i class="fa fa-times"></i>
                        </a>
                    </div>
                </div>

                <div class="ibox-content">
                    <div class="sk-spinner sk-spinner-double-bounce">
                        <div class="sk-double-bounce1"></div>
                        <div class="sk-double-bounce2"></div>
                    </div>
                    <div class="table-responsive">
                        <table id="journalVouchersGrid" class="table table-striped table-bordered table-hover" width="100%">
                            <tbody>
                                <tr>
                                    <td colspan="8" class="dataTables_empty">
                                        Loading data from server
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}

@section scripts {
    @Scripts.Render("~/plugins/dataTables")

    <script type="text/javascript">
        $(document).ready(function () {
            $('#journalVouchersGrid').DataTable({
                "sPaginationType": "full_numbers",
                "bProcessing": true,
                "bServerSide": true,
                "sAjaxSource": "@(Url.Action("Index", "BatchOrigination_Voucher", new { Area = "Accounts" }))",
                "sServerMethod": "POST",
                "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
                    var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
                    var h = ("0" + createdDate.getHours()).slice(-2);
                    var m = ("0" + createdDate.getMinutes()).slice(-2);
                    var s = ("0" + createdDate.getSeconds()).slice(-2);
                    var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
                    aData.CreatedDate = dt_Created;

                    var dropdownHtml = `
                        <div class="dropdown">
                            <button style="width: 150px;" class="btn btn-outline-info dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Action
                            </button>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">`;

                    var viewButton = `<a class="dropdown-item" href="/Accounts/BatchOrigination_Voucher/details/${aData.Id}">View</a>`;
                    var editButton = `<a class="dropdown-item" href="/Accounts/BatchOrigination_Voucher/edit/${aData.Id}">Edit</a>`;
                    var verifyButton = `<a class="dropdown-item" href="/Accounts/BatchOrigination_Voucher/Verify/${aData.Id}">Verify</a>`;
                    var authorizeButton = `<a class="dropdown-item" href="/Accounts/BatchOrigination_Voucher/Authorize/${aData.Id}">Authorize</a>`;

                    switch (aData.Status) {
                        case 1: // Pending
                            dropdownHtml += viewButton + editButton + verifyButton + authorizeButton;
                            break;
                        case 8: // Audited
                            dropdownHtml += viewButton + authorizeButton;
                            break;
                        case 2: // Posted
                            dropdownHtml += viewButton;
                            break;
                        default:
                            dropdownHtml += viewButton;
                            break;
                    }

                    dropdownHtml += `</div></div>`;

                    $('td:eq(6)', nRow).html(dt_Created);
                    $('td:eq(7)', nRow).html(dropdownHtml);

                    return nRow;
                },
                "aoColumns": [
                    { "mDataProp": "BranchDescription", "sTitle": "Branch", "sWidth": "15%" },
                    { "mDataProp": "VoucherNumber", "sTitle": "Voucher Number", "sWidth": "15%" },
                    { "mDataProp": "StatusDescription", "sTitle": "Status", "sWidth": "15%" },
                    { "mDataProp": "Remarks", "sTitle": "Remarks", "sWidth": "15%" },
                    { "mDataProp": "ChartOfAccountAccountType", "sTitle": "Type", "sWidth": "10%" },
                    { "mDataProp": "ChartOfAccountAccountName", "sTitle": "G/L Account Name", "sWidth": "12%" },
                    { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "12%" },
                    { "mDataProp": "CreatedDate", "sTitle": "Actions", "sWidth": "10%", "bSortable": false }
                ],
                "aaSorting": [[0, "desc"]]
            });
        });

        $(document).ajaxStart(function () {
            $('.ibox').children('.ibox-content').toggleClass('sk-loading');
        });

        $(document).ajaxStop(function () {
            $('.ibox').children('.ibox-content').toggleClass('sk-loading');
        });
    </script>
}
