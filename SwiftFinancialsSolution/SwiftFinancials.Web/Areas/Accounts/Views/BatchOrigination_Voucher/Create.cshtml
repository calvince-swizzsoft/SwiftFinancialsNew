@model  Application.MainBoundedContext.DTO.AccountsModule.JournalVoucherDTO

@{
    /**/

    ViewBag.Title = "Create Journal Voucher";
    var journalVoucherTypes = ViewBag.JournalVoucherTypeSelectList as List<SelectListItem>;
    var journalVoucherEntryTypes = ViewBag.JournalVoucherEntryTypeSelectList as List<SelectListItem>;
}

<style>
    .main {
        border: 1px solid gray;
        border-radius: 12px;

        font-weight:bold;
        color: #4d4e4f;
        background-color: #f1f1f1;
    }
    .custom-style {
        border: 4px solid #00ffff;
        border-radius: 12px;
    }
    .custom-style {
        padding: 10px;
        margin: 10px;
        font-size: 15px;
    }

    .journal-voucher-container {
        border: 3px solid #333;
        margin: 20px;
        padding: 20px;

        font-size: 15px;
        border-radius: 8px;
        position: relative;
        padding:20px; /* Space for the h2 */
        padding-top:40px;
        margin-bottom: 30px;
    }

        .journal-voucher-container h2 {
            position: absolute;
            top: -20px;
            left: 20px;
            background: #fff;
            padding: 0 10px;
            margin: 0;
        }

        .journal-voucher-container .form-control,
        .journal-voucher-container .input-group-addon {
            font-weight: normal; /* Retain normal weight for controls */
        }

    /* Ensure that lookups and date pickers retain their normal styles */
    .JournalVoucherEntryChartOfAccountsLookup,
    .BranchesLookup {
        font-weight: normal; /* Override bold for lookups */
    }

    .btn btn-success {
        padding: 10px;
        margin-left: 20px;
        margin-bottom: 10px;
    }
    @*css for the journal entries*@

    #ChargeSplits {
        width: 100%;
        border-collapse: collapse;
        font-family: Arial, sans-serif;
    }

        #ChargeSplits thead {
            background-color: white;
            color: #333;
            font-family: Arial, Helvetica, sans-serif;
        }

        #ChargeSplits th, #ChargeSplits td {
            padding: 12px;
            text-align: left;
            border: 1px solid #ddd;
        }

        #ChargeSplits th {
            font-weight: bold;
        }

        #ChargeSplits tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        #ChargeSplits tbody tr:hover {
            background-color: #f1f1f1;
        }

    .spacing-form-group {
        margin-bottom:10px; /* Adjust the value to increase or decrease spacing */
    }
    h2{
        font-weight:bold;
    }
    .form-control {
        color: #007bff;
    }
    input.form-control {
        color: #007bff; /* Replace with your desired color */
    }
    .bottom-button {
        border: 2px solid gray; /* Border color set to gray */
        padding: 10px; /* Padding inside the border */
        border-radius: 5px; /* Rounded corners (optional) */

        margin: 10px;
        font-size: 18px;
        font-weight: bold;
        background-color: #00e0ff;
    }


</style>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-12">
        <h2>Accounts</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            <li class="breadcrumb-item">
                @Html.ActionLink("Home", "Index", "BatchOrigination_VoucherController", new { Area = "Accounts" }, null)
            </li>
            <li class="breadcrumb-item active">
                <strong>Create</strong>
            </li>
        </ol>
    </div>
</div>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger fade in">
        <button class="close" data-dismiss="alert">
            ×
        </button>
        <i class="fa-fw fa fa-times"></i>
        <strong>Error!</strong> @TempData["Error"]
    </div>
}

<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h2>Journal Voucher</h2>

                    @if (TempData["ErrorMsg"] != null)
                    {
                        <div class="alert alert-danger">
                            <strong>Attempt Failed !</strong> @TempData["ErrorMsg"]
                        </div>
                    }

                    @if (TempData["EntryAdded"] != null)
                    {
                        <div class="alert alert-success">
                            <strong>Success !</strong> @TempData["EntryAdded"]

                            <button type="button" class="close" data-dismiss="alert">
                                <span>
                                    &times;
                                </span>
                            </button>
                        </div>
                    }

                    @if (TempData["tPercentage"] != null)
                    {
                        <div class="alert alert-danger">
                            <strong>Attempt Failed !</strong> @TempData["tPercentage"]
                        </div>
                    }

                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="close-link">
                            <i class="fa fa-times"></i>
                        </a>
                    </div>
                </div>
                <div class="ibox-content bg-white text-info">
                    <div class="main">
                        @using (Html.BeginForm("Add", "BatchOrigination_Voucher", new { Area = "Accounts" }, FormMethod.Post, new { @class = "wizard-big", @id = "Addform" }))
                        {

                            <div class="row custom-style">
                                <div class="col-lg-3">
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.TypeDescription, new { @class = "control-label" })
                                        <div class="input-group">
                                            <span class="input-group-addon"><i class="fa fa-bars fa-fw"></i></span>
                                            @Html.DropDownListFor(model => model.Type, journalVoucherTypes, new { @class = "form-control input-md" })
                                            @Html.ValidationMessageFor(model => model.Type)
                                        </div>
                                    </div>
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.ChartOfAccountId, htmlAttributes: new { @class = "control-label" })
                                        @Html.HiddenFor(model => model.ChartOfAccountId)
                                        @Html.ValidationMessageFor(model => model.ChartOfAccountId, "", new { @class = "text-danger" })
                                        @Html.EditorFor(model => model.ChartOfAccountAccountName, new { htmlAttributes = new { @class = "ChartOfAccountsLookup form-control", placeholder = "Enter space to autofill .." } })
                                    </div>
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.SecondaryDescription, htmlAttributes: new { @class = "control-label" })
                                        @Html.EditorFor(model => model.SecondaryDescription, new { htmlAttributes = new { @class = "form-control", @placeholder = "" } })
                                        @Html.ValidationMessageFor(model => model.SecondaryDescription, "", new { @class = "" })
                                    </div>

                                </div>
                                <div class="col-lg-3">
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.TotalValue, htmlAttributes: new { @class = "control-label" })
                                        @Html.EditorFor(model => model.TotalValue, new { htmlAttributes = new { @class = "form-control", @placeholder = "Kshs. 0.00", @required = "" } })
                                        @Html.ValidationMessageFor(model => model.TotalValue, "", new { @class = "text-danger" })
                                    </div>

                                    <div class="form-group spacing-form-group" id="data_3">
                                        @Html.LabelFor(model => model.ValueDate, htmlAttributes: new { @class = "" })
                                        <div class="input-group date">
                                            <span class="input-group-addon"><i class="fa fa-calendar"></i></span>
                                            <input type="text" name="valuedate" class="form-control" value="@DateTime.Today.AddDays(-30).ToString("dd/MM/yyyy")">
                                        </div>
                                    </div>
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.PrimaryDescription, htmlAttributes: new { @class = "control-label" })
                                        @Html.EditorFor(model => model.PrimaryDescription, new { htmlAttributes = new { @class = "form-control", @placeholder = "" } })
                                        @Html.ValidationMessageFor(model => model.PrimaryDescription, "", new { @class = "" })
                                    </div>
                                </div>
                                <div class="col-lg-3">
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.PostingPeriodId, htmlAttributes: new { @class = "control-label" })
                                        @Html.HiddenFor(model => model.PostingPeriodId)
                                        @Html.ValidationMessageFor(model => model.PostingPeriodDescription, "", new { @class = "text-danger" })
                                        @Html.EditorFor(model => model.PostingPeriodDescription, new { htmlAttributes = new { @class = "PostingPeriodsLookup form-control required", placeholder = "Enter space to autofill.." } })
                                    </div>
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.Reference, htmlAttributes: new { @class = "control-label" })
                                        @Html.EditorFor(model => model.Reference, new { htmlAttributes = new { @class = "form-control", @placeholder = "", @required = "" } })
                                        @Html.ValidationMessageFor(model => model.Reference, "", new { @class = "text-danger" })
                                    </div>


                                </div>

                                <div class="col-lg-3">
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.BranchId, htmlAttributes: new { @class = "control-label" })
                                        @Html.HiddenFor(model => model.BranchId)
                                        @Html.ValidationMessageFor(model => model.BranchDescription, "", new { @class = "text-danger" })
                                        @Html.EditorFor(model => model.BranchDescription, new { htmlAttributes = new { @class = "BranchesLookup form-control required", placeholder = "Enter space to autofill.." } })
                                    </div>
                                    <div class="form-group form-group-lg spacing-form-group">
                                        @Html.LabelFor(model => model.Remarks, htmlAttributes: new { @class = "control-label" })
                                        @Html.EditorFor(model => model.Remarks, new { htmlAttributes = new { @class = "form-control", @placeholder = "" } })
                                        @Html.ValidationMessageFor(model => model.Remarks, "", new { @class = "" })
                                    </div>
                                </div>

                            </div>




                            <br />

                            <div class="journal-voucher-container">
                                <h2>Add Journal Voucher Entry</h2>
                                <div class="row">
                                    <div class="col-lg-3">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.JournalVoucherEntries[0].EntryType, new { @class = "control-label" })
                                            <div class="input-group">
                                                <span class="input-group-addon"><i class="fa fa-bars fa-fw"></i></span>
                                                @Html.DropDownListFor(model => model.JournalVoucherEntries[0].EntryType, journalVoucherEntryTypes, new { @class = "form-control input-md entry-field" })
                                                @Html.ValidationMessageFor(model => model.JournalVoucherEntries[0].EntryType)
                                            </div>
                                        </div>
                                    </div>

                                    <div class="col-lg-3">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.JournalVoucherEntries[0].ChartOfAccountAccountName, htmlAttributes: new { @class = "control-label" })
                                            @Html.HiddenFor(model => model.JournalVoucherEntries[0].ChartOfAccountId, new { @id = "JournalVoucherEntriesChartOfAccountId" })
                                            @Html.ValidationMessageFor(model => model.JournalVoucherEntries[0].ChartOfAccountAccountName, "", new { @class = "text-danger" })
                                            @Html.EditorFor(model => model.JournalVoucherEntries[0].ChartOfAccountAccountName, new { htmlAttributes = new { @class = "JournalVoucherEntryChartOfAccountsLookup form-control required entry-field", placeholder = "Enter space to autofill.." } })
                                        </div>
                                    </div>

                                    <div class="col-lg-3">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.JournalVoucherEntries[0].Reference, htmlAttributes: new { @class = "control-label" })
                                            @Html.EditorFor(model => model.JournalVoucherEntries[0].Reference, new { htmlAttributes = new { @class = "form-control entry-field", @placeholder = "" } })
                                            @Html.ValidationMessageFor(model => model.JournalVoucherEntries[0].Reference, "", new { @class = "text-danger" })
                                        </div>
                                    </div>

                                    <div class="col-lg-3">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.JournalVoucherEntries[0].Amount, htmlAttributes: new { @class = "control-label" })
                                            @Html.EditorFor(model => model.JournalVoucherEntries[0].Amount, new { htmlAttributes = new { @class = "form-control entry-field", @placeholder = "Ksh", @required = "" } })
                                            @Html.ValidationMessageFor(model => model.JournalVoucherEntries[0].Amount, "", new { @class = "text-danger" })
                                        </div>
                                    </div>


                                </div>




                                <div class="row">
                                    <!-- Center the Submit button -->
                                    <div style="text-align: right; margin: 20px 20px;">
                                        <input type="submit" value="Add" class="btn btn-primary" style="width: 150px; border-radius: 4px;" />
                                    </div>

                                </div>

                                <br />

                                <row>

                                    <table id="ChargeSplits" class="footable table table-striped toggle-arrow-tiny grey-table" style="text-wrap: normal; color: black; width: 100%; margin-bottom: 50px;">
                                        <thead style="font-style: italic; color: cadetblue; text-align: center;">
                                            <tr>
                                                <th style="width: 15%;">Entry Type</th>
                                                <th style="width: 15%;">Chart of Account</th>
                                                <th style="width: 5%;">Reference</th>
                                                <th style="width: 10%;">Amount</th>
                                                <th style="width: 5%;">Action</th>

                                            </tr>
                                        </thead>
                                        <tbody>
                                        </tbody>
                                    </table>
                                </row>
                            </div>





                        }


                        @using (Html.BeginForm("Create", "BatchOrigination_Voucher", new { Area = "Accounts" }, FormMethod.Post, new { @class = "wizard-big", @id = "voucherForm" }))
                        {
                            <div class="bottom-button">
                                <div class="row">
                                    <div class="col-lg-12" style="display: flex; justify-content: flex-end;">
                                        <div class="form-group form-group-sm">
                                            <input type="submit" value="Create" class="btn btn-primary" />
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }


                    </div>

                </div>

            </div>
        </div>
    </div>
</div>





@section Styles{
    @Styles.Render("~/Content/plugins/iCheck/iCheckStyles")
    @Styles.Render("~/plugins/wizardStepsStyles")
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/plugins/awesomeCheckboxStyles")
    @Styles.Render("~/plugins/clockpickerStyles")
    @Styles.Render("~/plugins/dateRangeStyles")
    @Styles.Render("~/Content/plugins/chosen/chosenStyles")
    @Styles.Render("~/plugins/switcheryStyles")
    @Styles.Render("~/plugins/jasnyBootstrapStyles")
    @Styles.Render("~/plugins/nouiSliderStyles")
    @Styles.Render("~/plugins/dataPickerStyles")
    @Styles.Render("~/Content/plugins/ionRangeSlider/ionRangeStyles")
    @Styles.Render("~/plugins/imagecropperStyles")
    @Styles.Render("~/Content/plugins/colorpicker/colorpickerStyles")
    @Styles.Render("~/plugins/select2Styles")
    @Styles.Render("~/plugins/touchSpinStyles")
    @Styles.Render("~/plugins/tagInputsStyles")
    @Styles.Render("~/plugins/duallistStyles")
}

@section Scripts{
    @Scripts.Render("~/plugins/wizardSteps")
    @Scripts.Render("~/plugins/validate")
    @Scripts.Render("~/plugins/typehead")
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/iCheck")
    @Scripts.Render("~/plugins/dataPicker")
    @Scripts.Render("~/plugins/ionRange")
    @Scripts.Render("~/plugins/nouiSlider")
    @Scripts.Render("~/plugins/jasnyBootstrap")
    @Scripts.Render("~/plugins/switchery")
    @Scripts.Render("~/plugins/chosen")
    @Scripts.Render("~/plugins/knob")
    @Scripts.Render("~/plugins/imagecropper")
    @Scripts.Render("~/plugins/colorpicker")
    @Scripts.Render("~/plugins/clockpicker")
    @Scripts.Render("~/plugins/dateRange")
    @Scripts.Render("~/plugins/select2")
    @Scripts.Render("~/plugins/touchSpin")
    @Scripts.Render("~/plugins/tagInputs")
    @Scripts.Render("~/plugins/duallist")

    <script>
         //Create form submission

    $(document).ready(function () {
        $('#voucherForm').on('submit', function (e) {
            e.preventDefault(); // Prevent the default form submission

            // Serialize the form data
            var formData = $(this).serialize();

            $.ajax({
                url: '@Url.Action("Create", "BatchOrigination_Voucher", new { Area = "Accounts" })',
                type: 'POST',
                data: formData,
                success: function (response) {
                    if (response.success) {
                        // Show success message and redirect
                        Swal.fire({
                            icon: 'success',
                            title: 'Success!',
                            text: response.message,
                            timer: 1000,
                            showConfirmButton: false
                        }).then(function () {
                            // Redirect to the Index page of the same controller
                            window.location.href = '@Url.Action("Index", "BatchOrigination_Voucher", new { Area = "Accounts" })';
                        });
                    } else {
                        // Show error message
                        Swal.fire({
                            icon: 'error',
                            title: 'Error!',
                            text: response.message
                        });
                    }
                },
                error: function (xhr, status, error) {
                    // Handle AJAX errors
                    Swal.fire({
                        icon: 'error',
                        title: 'Oops...',
                        text: 'Something went wrong!'
                    });
                }
            });
        });
    });
    </script>




    





    <script>

       
    $(document).ready(function () {
        $('#Addform').submit(function (event) {
            event.preventDefault(); // Prevent the form from submitting normally

            var formData = $(this).serialize(); // Serialize form data

            $.ajax({
                url: '@Url.Action("Add", "BatchOrigination_Voucher", new { Area = "Accounts" })',
                type: 'POST',
                data: formData,
                success: function (response) {
                    if (response.success) {
                        updateEntriesTable(response.entries);
                        Swal.fire({
                            icon: 'success',
                            title: 'Success!',
                            text: 'Entry added successfully.',
                            timer: 1000,
                            showConfirmButton: false
                        });
                        clearEntryForm();
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error!',
                            text: response.message,
                            timer: 8000,
                            showConfirmButton: true
                        });

                    }
                },
                error: function () {
                    alert('An error occurred while adding the entry.');
                }
            });
        });
    });




        function updateEntriesTable(entries) {
            var tableBody = $("#ChargeSplits tbody");
            tableBody.empty(); // Clear existing entries

            $.each(entries, function (index, entry) {
                var row = `<tr data-id="${entry.Id}">
                       <td>${entry.EntryTypeDescription}</td>
                       <td>${entry.ChartOfAccountAccountName}</td>
                       <td>${entry.Reference}</td>
                       <td>${entry.Amount}</td>
                       <td><button type="button" class="btn btn-danger remove-entry" data-id="${entry.Id}">Remove</button></td>
                   </tr>`;
                tableBody.append(row);
            });
        }


        function clearEntryForm() {
            // Reset only the fields with the 'entry-field' class
            $('#Addform').find('.entry-field').val('');
        }




        // Attach click event to remove buttons
        $(document).on("click", ".remove-entry", function (event) {
            event.preventDefault(); // Prevent default behavior
            var entryId = $(this).data("id"); // Get the GUID from the button's data-id
            removeEntry(entryId);
        });



        function removeEntry(id) {
            $.ajax({
                url: '@Url.Action("Remove", "BatchOrigination_Voucher")',
                type: 'POST',
                data: { id: id },
                success: function (response) {
                    if (response.success) {
                        updateEntriesTable(response.data);
                    } else {
                        alert("An error occurred while removing the entry.");
                    }
                },
                error: function () {
                    alert("An error occurred while processing the request.");
                }
            });
        }


    </script>




    <script>

     $(document).ready(function () {
            $('.remove-btn').click(function () {
                var row = $(this).closest('tr');
                var id = row.data('id');


                $.ajax({
                    url: '@Url.Action("removeChargeSplit", "BatchOrigination_Voucher")',
                    type: 'POST',
                    data: { id: id },
                    success: function () {
                        row.remove();
                    },
                    error: function () {
                        alert('Error removing row.');
                    }
                });
            });
     });




    // Open the modal
    $("#openModalButton").click(function () {
        $("#journalModal").css("display", "block");
    });

    // Close the modal
    $(".close").click(function () {
        $("#journalModal").css("display", "none");
    });

    $("#submitEntry").click(function () {
        const type = $("#type").val();
        const chartOfAccountId = $("#chartOfAccountId").val();
        const branchId = $("#branchId").val();
        const postingPeriodId = $("#postingPeriodId").val();
        const totalValue = $("#totalValue").val();
        const primaryDescription = $("#primaryDescription").val();
        const secondaryDescription = $("#secondaryDescription").val();
        const reference = $("#reference").val();
        const valueDate = $("#valueDate").val();
        const remarks = $("#remarks").val();

        // Perform AJAX request to send the data to your server
        $.post("/addJournalVoucherEntry", { type, chartOfAccountId, branchId, postingPeriodId, totalValue, primaryDescription, secondaryDescription, reference,valueDate,remarks }, function (response) {
            // Handle the response from the server
        });
    });

    //ChartOfAccount typehead starts here
    $('.JournalVoucherEntryChartOfAccountsLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetChartOfAccountsAsync", "ChartOfAccount", new { Area = "Accounts" })',
                        { query: query },
                        function (ChartOfAccounts) {
                            var newChartOfAccountsData = [];
                            ChartOfAccountsMap = {};
                            $.each(ChartOfAccounts, function (i, ChartOfAccount) {
                                newChartOfAccountsData.push(ChartOfAccount.AccountName);
                                ChartOfAccountsMap[ChartOfAccount.AccountName] = ChartOfAccount;
                            });
                            process(newChartOfAccountsData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var chartOfAccountId = "";
                    ChartOfAccountId = ChartOfAccountsMap[item].Id;
                    $('#JournalVoucherEntriesChartOfAccountId').val(ChartOfAccountId);
                    //console.log(ChartOfAccountId)
                    return item;
                }
    });



        //PostingPeriod typehead starts here
    $('.JournalVoucherEntryPostingPeriodsLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetPostingPeriodsAsync", "PostingPeriod", new { Area = "Accounts" })',
                        { query: query },
                        function (PostingPeriods) {
                            var newPostingPeriodsData = [];
                            PostingPeriodsMap = {};
                            $.each(PostingPeriods, function (i, PostingPeriod) {
                                newPostingPeriodsData.push(PostingPeriod.Description);
                                PostingPeriodsMap[PostingPeriod.Description] = PostingPeriod;
                            });
                            process(newPostingPeriodsData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var postingPeriodId = "";
                    PostingPeriodId = PostingPeriodsMap[item].Id;
                    $('#Id').val(PostingPeriodId);
                    //console.log(PostingPeriodId)
                    return item;
                }
    });

    //ChartOfAccount typehead starts here
    $('.ChartOfAccountsLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetChartOfAccountsAsync", "ChartOfAccount", new { Area = "Accounts" })',
                        { query: query },
                        function (ChartOfAccounts) {
                            var newChartOfAccountsData = [];
                            ChartOfAccountsMap = {};
                            $.each(ChartOfAccounts, function (i, ChartOfAccount) {
                                newChartOfAccountsData.push(ChartOfAccount.AccountName);
                                ChartOfAccountsMap[ChartOfAccount.AccountName] = ChartOfAccount;
                            });
                            process(newChartOfAccountsData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var chartOfAccountId = "";
                    ChartOfAccountId = ChartOfAccountsMap[item].Id;
                    $('#ChartOfAccountId').val(ChartOfAccountId);
                    //console.log(ChartOfAccountId)
                    return item;
                }
            });

             //PostingPeriod typehead starts here
            $('.PostingPeriodsLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetPostingPeriodsAsync", "PostingPeriod", new { Area = "Accounts" })',
                        { query: query },
                        function (PostingPeriods) {
                            var newPostingPeriodsData = [];
                            PostingPeriodsMap = {};
                            $.each(PostingPeriods, function (i, PostingPeriod) {
                                newPostingPeriodsData.push(PostingPeriod.Description);
                                PostingPeriodsMap[PostingPeriod.Description] = PostingPeriod;
                            });
                            process(newPostingPeriodsData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var postingPeriodId = "";
                    PostingPeriodId = PostingPeriodsMap[item].Id;
                    $('#PostingPeriodId').val(PostingPeriodId);
                    //console.log(PostingPeriodId)
                    return item;
                }
            });

         //Branch typehead starts here
            $('.BranchesLookup').typeahead({
                source: function (query, process) {
                    return $.getJSON(
                        '@Url.Action("GetBranchesAsync", "Branch", new { Area = "Admin" })',
                        { query: query },
                        function (Branches) {
                            var newBranchesData = [];
                            BranchesMap = {};
                            $.each(Branches, function (i, Branch) {
                                newBranchesData.push(Branch.Description);
                                BranchesMap[Branch.Description] = Branch;
                            });
                            process(newBranchesData); return;
                        });
                },
                matcher: function (item) {
                    if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                        return true;
                    }
                },
                sorter: function (items) {
                    return items.sort();
                },
                highlighter: function (item) {
                    var regex = new RegExp('(' + this.query + ')', 'gi');
                    return item.replace(regex, "<strong>$1</strong>");
                },
                updater: function (item) {
                    var branchId = "";
                    BranchId = BranchesMap[item].Id;
                    $('#BranchId').val(BranchId);
                    //console.log(BranchId)
                    return item;
                }
            });
        $(document).ready(function () {
            $('#data_3 .input-group.date').datepicker({
                startView: 2,
                todayBtn: "linked",
                keyboardNavigation: false,
                forceParse: false,
                autoclose: true,
            });
        });

    </script>
}
