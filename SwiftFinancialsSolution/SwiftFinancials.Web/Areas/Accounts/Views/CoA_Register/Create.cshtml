@model  Application.MainBoundedContext.DTO.AccountsModule.CustomerAccountDTO

@{
    /**/

    ViewBag.Title = "Create Customer Accounts";
    var withdrawalNotificationCategories = ViewBag.WithdrawalNotificationCategorySelectList as List<SelectListItem>
    ;
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-12">
        <h1>Accounts</h1>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            <li class="breadcrumb-item">
                @Html.ActionLink("Home", "Index", "CoA_Register", new { Area = "Accounts" }, null)
            </li>
            <li class="breadcrumb-item active">
                <strong>Create</strong>
            </li>
        </ol>
    </div>
</div>

<style>
    body {
        font-family: Arial, sans-serif;
    }

    .tabs {
        display: flex;
        border-bottom: 1px solid #ccc;
        margin-bottom: 10px;
        width: 100%;
    }

    .tab {
        background-color: #f1f1f1;
        border: none;
        padding: 10px 20px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

        .tab:hover {
            background-color: #ddd;
        }

        .tab.active {
            background-color: #ddd; /* Same as hover background color */
        }

    .tab-content {
        max-height: 600px; /* Adjust the height as needed */
        overflow-y: auto;
        padding: 10px;
        border: 1px solid #ccc; /* Borderline around tab content */
        background-color: #fff;
        margin-top: 10px;
        display: none;
    }

        .tab-content.active {
            display: block;
        }
</style>


@if (TempData["Error"] != null)
{
    <div class="alert alert-danger fade in">
        <button class="close" data-dismiss="alert">
            ×
        </button>
        <i class="fa-fw fa fa-times"></i>
        <strong>Error!</strong> @TempData["Error"]
    </div>
}

<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h2>Register New Customer Account</h2>
                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="close-link">
                            <i class="fa fa-times"></i>
                        </a>
                    </div>
                </div>
                <div class="ibox-content bg-white">
                    @using (Html.BeginForm("Create", "CoA_Register", new { Area = "Accounts" }, FormMethod.Post, new { @class = "wizard-big", @id = "form" }))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                            <fieldset>
                                <div class="row">
                                    <div class="col-lg-4">
                                        <div class="col-md-13 form-group">
                                            @Html.LabelFor(model => model.CustomerId, new { @class = "control-label" })
                                            <div class="input-group" data-autoclose="true">
                                                @Html.HiddenFor(model => model.CustomerId)
                                                @Html.TextBoxFor(model => model.CustomerIndividualFirstName, new { @class = "form-control input-md alpha alphaspace required", @readonly = "readonly", @placeholder = "Link Customer..." })
                                                <span class="input-group-addon">
                                                    <a data-toggle="modal" data-target="#customerLookupModal"><span class="fa fa-search"></span></a>
                                                </span>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="col-lg-4">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.Customers.StationZoneDivisionEmployerDescription, htmlAttributes: new { @class = "" })
                                            @Html.HiddenFor(model => model.Customers.StationZoneDivisionEmployerId)
                                            @Html.EditorFor(model => model.Customers.StationZoneDivisionEmployerDescription, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @placeholder = "Employer..." } })
                                            @Html.ValidationMessageFor(model => model.Customers.StationZoneDivisionEmployerDescription, "", new { @class = "text-danger" })
                                        </div>
                                    </div>

                                    <div class="col-lg-4">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.Customers.StationDescription, htmlAttributes: new { @class = "" })
                                            @Html.HiddenFor(model => model.Customers.StationId)
                                            @Html.EditorFor(model => model.Customers.StationDescription, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @placeholder = "Station..." } })
                                            @Html.ValidationMessageFor(model => model.Customers.StationDescription, "", new { @class = "text-danger" })
                                        </div>
                                    </div>
                                </div>


                            </fieldset>
                        </div>






                    }

                    @using (Html.BeginForm("Add", "CoA_Register", new { Area = "Accounts" }, FormMethod.Post, new { @class = "wizard-big", @id = "form" }))
                    {
                        <br />
                        <hr />


                        @*End chargeType Splitting*@

                        <br />


                        @*<h2 style="color: cadetblue;">Link Products</h2>*@

                        <br />



                        @*Tabs*@
                        <div class="tab-header" style="color: black;">
                            <div class="tab-button active" onclick="openTab(event, 'section1')">
                                <i class="fa fa-table" aria-hidden="true"></i> Saving Products
                            </div>

                            <div class="tab-button" onclick="openTab(event, 'section2')">
                                <i class="fa fa-table" aria-hidden="true"></i> Investments Products
                            </div>


                        </div>
                        <br />

                        <div id="section2" class="tab">

                            <div class="tab-content">

                                <h2 style="color: cadetblue;">Investments Products</h2>

                                <br />

                                <div class="row">
                                    <div class="col-lg-4">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.Customers.PaddedSerialNumber, htmlAttributes: new { @class = "" })
                                            @Html.HiddenFor(model => model.Customers.PaddedSerialNumber)
                                            @Html.EditorFor(model => model.Customers.PaddedSerialNumber, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @placeholder = "Serial Number..." } })
                                            @Html.ValidationMessageFor(model => model.Customers.PaddedSerialNumber, "", new { @class = "text-danger" })
                                        </div>
                                    </div>

                                    <div class="col-md-13 form-group">
                                        @Html.LabelFor(model => model.invest[0].Id, new { @class = "control-label" })
                                        <div class="input-group" data-autoclose="true">
                                            @Html.HiddenFor(model => model.invest[0].Id)
                                            @Html.TextBoxFor(model => model.invest[0].Description, new { @class = "form-control input-md alpha alphaspace required", @readonly = "readonly" })
                                            <span class="input-group-addon">
                                                <a data-toggle="modal" data-target="#investmentsProductLookupModal"><span class="fa fa-search"></span></a>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-lg-4">
                                    <div class="form-group form-group-lg">
                                        @Html.LabelFor(model => model.invest[0].ChartOfAccountAccountName, htmlAttributes: new { @class = "" })
                                        @Html.EditorFor(model => model.invest[0].ChartOfAccountAccountName, new { htmlAttributes = new { @class = "form-control" } })
                                        @Html.ValidationMessageFor(model => model.invest[0].ChartOfAccountAccountName, "", new { @class = "text-danger" })
                                    </div>
                                </div>

                                <div class="col-lg-4">
                                    <div class="form-group form-group-lg">
                                        @Html.LabelFor(model => model.Customers.IndividualPayrollNumbers, htmlAttributes: new { @class = "" })
                                        @Html.HiddenFor(model => model.Customers.IndividualPayrollNumbers)
                                        @Html.EditorFor(model => model.Customers.IndividualPayrollNumbers, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @placeholder = "Payroll Number..." } })
                                        @Html.ValidationMessageFor(model => model.Customers.IndividualPayrollNumbers, "", new { @class = "text-danger" })
                                        @Html.LabelFor(model => model.invest[0].Code, htmlAttributes: new { @class = "" })
                                        @Html.EditorFor(model => model.invest[0].Code, new { htmlAttributes = new { @class = "form-control percentage-input" } })
                                        @Html.ValidationMessageFor(model => model.invest[0].Code, "", new { @class = "text-danger" })
                                        <div class="invalid-feedback" style="display: none; color:red;"></div>
                                    </div>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-lg-4">
                                    <div class="form-group form-group-lg">
                                        <div class="col-md-13 form-group">
                                            @Html.LabelFor(model => model.Savings.Id, new { @class = "control-label" })
                                            <div class="input-group" data-autoclose="true">
                                                @Html.HiddenFor(model => model.Savings.Id)
                                                @Html.TextBoxFor(model => model.Savings.Description, new { @class = "form-control input-md alpha alphaspace required", @readonly = "readonly" })
                                                <span class="input-group-addon">
                                                    <a data-toggle="modal" data-target="#savingsLookupModal"><span class="fa fa-search"></span></a>
                                                </span>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="col-lg-4">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.Customers.Reference2, htmlAttributes: new { @class = "" })
                                            @Html.HiddenFor(model => model.Customers.Reference2)
                                            @Html.EditorFor(model => model.Customers.Reference2, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @placeholder = "Membership Number..." } })
                                            @Html.ValidationMessageFor(model => model.Customers.Reference2, "", new { @class = "text-danger" })
                                        </div>
                                    </div>

                                    <div class="col-lg-4">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.Customers.Reference3, htmlAttributes: new { @class = "" })
                                            @Html.HiddenFor(model => model.Customers.Reference3)
                                            @Html.EditorFor(model => model.Customers.Reference3, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @placeholder = "Personal File Number..." } })
                                            @Html.ValidationMessageFor(model => model.Customers.Reference3, "", new { @class = "text-danger" })
                                        </div>
                                    </div>
                                </div>

                                <br />

                                @*Tabs*@
                                <div class="tabs">
                                    <div class="tab" onclick="openTab('section1')">
                                        <i class="fa fa-table" aria-hidden="true"></i> Savings Products
                                    </div>
                                    <div class="tab" onclick="openTab('section2')">
                                        <i class="fa fa-table" aria-hidden="true"></i> Loans Products
                                    </div>
                                    <div class="tab" onclick="openTab('section3')">
                                        <i class="fa fa-table" aria-hidden="true"></i> Investments Products
                                    </div>
                                </div>

                                @*Savings Products*@
                                <div id="section1" class="tab-content">
                                    <div class="form-group">
                                        <div style="text-align: left;">
                                            <input type="checkbox" id="sheaderCheckbox" style="transform: scale(1); color: blue;">
                                            <label for="sheaderCheckbox" style="font-weight: bold;">Select All</label>
                                        </div>

                                        <div class="table-responsive">
                                            <table id="SavingsProductsGrid" class="table table-striped table-bordered table-hover" width="100%">
                                                <tbody>
                                                    <tr>
                                                        <td colspan="7" class="dataTables_empty">
                                                            Loading data from server
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>


                                @*Loans Products*@
                                <div id="section2" class="tab-content">
                                    <div class="form-group">
                                        <div style="text-align: left;">
                                            <input type="checkbox" id="lheaderCheckbox" style="transform: scale(1); color: blue;">
                                            <label for="lheaderCheckbox" style="font-weight: bold;">Select All</label>
                                        </div>

                                        <div class="table-responsive">
                                            <table id="LoanProductsGrid" class="table table-striped table-bordered table-hover" width="100%">
                                                <tbody>
                                                    <tr>
                                                        <td colspan="7" class="dataTables_empty">
                                                            Loading data from server
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>



                                @*Investments Products*@
                                <div id="section3" class="tab-content">
                                    <div class="form-group">
                                        <div style="text-align: left;">
                                            <input type="checkbox" id="iheaderCheckbox" style="transform: scale(1); color: blue;">
                                            <label for="iheaderCheckbox" style="font-weight: bold;">Select All</label>
                                        </div>

                                        <div class="table-responsive">
                                            <table id="InvestmentsProductsGrid" class="table table-striped table-bordered table-hover" width="100%">
                                                <tbody>
                                                    <tr>
                                                        <td colspan="7" class="dataTables_empty">
                                                            Loading data from server
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>

                                <br />

                                <div class="row">
                                    <div class="col-lg-4">
                                        <button id="submit-btn" class="btn btn-primary" style="margin-top: 20px; margin-bottom: 20px; width: 150px;">Create</button>
                                    </div>
                                </div>


                            </div>

                        </div>

                    }
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal  fade" id="customerLookupModal" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header bg-primary">
                <h3 class="font-bold">Customers Lookup<button type="button" class="close" data-dismiss="modal"><span class="sr-only">Close</span></button></h3>
            </div>
            <table id="customersGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                <tbody>
                    <tr>
                        <td class="dataTables_empty">
                            Loading data from server
                        </td>
                    </tr>
                </tbody>
            </table>

            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>










@section Styles{
    @Styles.Render("~/Content/plugins/iCheck/iCheckStyles")
    @Styles.Render("~/plugins/wizardStepsStyles")
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/plugins/awesomeCheckboxStyles")
    @Styles.Render("~/plugins/clockpickerStyles")
    @Styles.Render("~/plugins/dateRangeStyles")
    @Styles.Render("~/Content/plugins/chosen/chosenStyles")
    @Styles.Render("~/plugins/switcheryStyles")
    @Styles.Render("~/plugins/jasnyBootstrapStyles")
    @Styles.Render("~/plugins/nouiSliderStyles")
    @Styles.Render("~/plugins/dataPickerStyles")
    @Styles.Render("~/Content/plugins/ionRangeSlider/ionRangeStyles")
    @Styles.Render("~/plugins/imagecropperStyles")
    @Styles.Render("~/Content/plugins/colorpicker/colorpickerStyles")
    @Styles.Render("~/plugins/select2Styles")
    @Styles.Render("~/plugins/touchSpinStyles")
    @Styles.Render("~/plugins/tagInputsStyles")
    @Styles.Render("~/plugins/duallistStyles")
}

@section Scripts{
    @Scripts.Render("~/plugins/wizardSteps")
    @Scripts.Render("~/plugins/validate")
    @Scripts.Render("~/plugins/typehead")
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/iCheck")
    @Scripts.Render("~/plugins/dataPicker")
    @Scripts.Render("~/plugins/ionRange")
    @Scripts.Render("~/plugins/nouiSlider")
    @Scripts.Render("~/plugins/jasnyBootstrap")
    @Scripts.Render("~/plugins/switchery")
    @Scripts.Render("~/plugins/chosen")
    @Scripts.Render("~/plugins/knob")
    @Scripts.Render("~/plugins/imagecropper")
    @Scripts.Render("~/plugins/colorpicker")
    @Scripts.Render("~/plugins/clockpicker")
    @Scripts.Render("~/plugins/dateRange")
    @Scripts.Render("~/plugins/select2")
    @Scripts.Render("~/plugins/touchSpin")
    @Scripts.Render("~/plugins/tagInputs")
    @Scripts.Render("~/plugins/duallist")

    <script>
    $('#customersGrid').DataTable({
                // Ajax settings
                "sPaginationType": "full_numbers",
                "bProcessing": true,
                "bServerSide": true,
                "sAjaxSource": "@(Url.Action("Index", "Customer", new { Area = "Registry" }))",
                "sServerMethod": "POST",
                // Callback settings

                "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {

                   var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
                    var h = ("0" + createdDate.getHours()).slice(-2);
                    var m = ("0" + createdDate.getMinutes()).slice(-2);
                    var s = ("0" + createdDate.getSeconds()).slice(-2);

                    var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
                    aData.CreatedDate = dt_Created;

                    var selectUrl = '<a  href="/Accounts/CoA_Register/Create/' + aData.Id + '" class = "btn btn-outline-info"">' + 'Select</a>';

                    $('td:eq(4)', nRow).html(dt_Created);
                    $('td:eq(5)', nRow).html(selectUrl);

                    return nRow;
                },
        // Display settings
        "aoColumns": [
            { "mDataProp": "FullName", "sTitle": "Name", "sWidth": "15%" },
            { "mDataProp": "TypeDescription", "sTitle": "Type", "sWidth": "10%" },
            { "mDataProp": "Reference2", "sTitle": "Membership  Number", "sWidth": "10%" },
            { "mDataProp": "IndividualTypeDescription", "sTitle": "Individual Type", "sWidth": "15%" },
            { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "10%" },
            { "mDataProp": "CreatedDate", "sTitle": "", "sWidth": "10%", "bSortable": false }],
        "aaSorting": [[0, "desc"]]
    });








        function openTab(tabId) {
            // Hide all tab content
            var i, tabcontent, tabs;
            tabcontent = document.getElementsByClassName("tab-content");
            for (i = 0; i < tabcontent.length; i++) {
                tabcontent[i].style.display = "none";
            }

            // Remove "active" class from all tabs
            tabs = document.getElementsByClassName("tab");
            for (i = 0; i < tabs.length; i++) {
                tabs[i].classList.remove("active");
            }

            // Show the current tab content
            document.getElementById(tabId).style.display = "block";

            // Add "active" class to the clicked tab
            var activeTab = Array.from(tabs).find(tab => tab.innerText === document.querySelector(`[onclick="openTab('${tabId}')"]`).innerText);
            if (activeTab) {
                activeTab.classList.add("active");
            }
        }

        // Set default tab
        document.querySelector(".tab").click();

    </script>






    @*Savings Products*@
    <script>
        $(document).ready(function () {
    // Function to save the state of the header checkbox to local storage
    function saveCheckboxState(key, state) {
        // Implementation for saving checkbox state goes here
    }

    // Function to load the state of the header checkbox from local storage
    function loadCheckboxState(key) {
        // Implementation for loading checkbox state goes here
    }

    $(document).ready(function () {
        // Event listener for header checkbox
        $('#sheaderCheckbox').on('change', function () {
            // Check or uncheck all row checkboxes based on the header checkbox state
            $('.srowCheckbox').prop('checked', $(this).prop('checked')).each(function () {
                var checkboxKey = $(this).attr('id');
                // Save the state of the checkbox to local storage
                saveCheckboxState(checkboxKey, $(this).prop('checked'));
            });
            // Save the state of the header checkbox to local storage
            saveCheckboxState('sheaderCheckbox', $(this).prop('checked'));
        });

        // Event listener for individual row checkboxes
        $('#SavingsProductsGrid').on('change', '.srowCheckbox', function () {
            // Save the state of the checkbox to local storage
            var checkboxKey = $(this).attr('id');
            saveCheckboxState(checkboxKey, $(this).prop('checked'));

            // Uncheck header checkbox if any row checkbox is unchecked
            if (!$(this).prop('checked')) {
                $('#sheaderCheckbox').prop('checked', false);
                // Save the state of the header checkbox to local storage
                saveCheckboxState('sheaderCheckbox', false);
            } else {
                // Check if all row checkboxes are checked and check the header checkbox accordingly
                var allChecked = $('.srowCheckbox:checked').length === $('.srowCheckbox').length;
                $('#sheaderCheckbox').prop('checked', allChecked);
                // Save the state of the header checkbox to local storage
                saveCheckboxState('sheaderCheckbox', allChecked);
            }
        });

        // Load the state of the header checkbox from local storage after the page is loaded
        $('#sheaderCheckbox').prop('checked', loadCheckboxState('sheaderCheckbox'));
    });

    // Initialize the DataTable
            var table = $('#SavingsProductsGrid').DataTable({
        // Ajax settings
        "sPaginationType": "full_numbers",
        "bProcessing": true,
        "bServerSide": true,
        "sAjaxSource": "@(Url.Action("Index", "SavingsProduct", new { Area = "Accounts"}))",

        "sServerMethod": "POST",
        // Callback settings
        "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
            var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
            var h = ("0" + createdDate.getHours()).slice(-2);
            var m = ("0" + createdDate.getMinutes()).slice(-2);
            var s = ("0" + createdDate.getSeconds()).slice(-2);
            var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
            aData.CreatedDate = dt_Created;

            //var editUrl = '<a href="/accounts/SavingsProduct/edit/' + aData.Id + '" class="btn btn-outline-info">Edit</a>';
            //var viewUrl = '<a href="/accounts/SavingsProduct/details/' + aData.Id + '" class="btn btn-outline-info">View</a>';
            var checkboxHTML = '<input type="checkbox" class="srowCheckbox" data-id="' + aData.Id + '"';
            if (aData.AutomateLedgerFeeCalculation === true) {
                checkboxHTML += ' checked';
            }
            checkboxHTML += '>';

            $('td:eq(0)', nRow).html(checkboxHTML);

            return nRow;
        },
        // Display settings
        "aoColumns": [
            { "mDataProp": "Id", "sTitle": "Select", "sWidth": "0.09%" },
            { "mDataProp": "Description", "sTitle": "Name", "sWidth": "10%", "bSortable": false },
            { "mDataProp": "MinimumBalance", "sTitle": "Minimum Balance", "sWidth": "7%", "bSortable": false },
            { "mDataProp": "OperatingBalance", "sTitle": "Operating Balance", "sWidth": "5%", "bSortable": false },
            { "mDataProp": "AnnualPercentageYield", "sTitle": "Annual Percentage Yield", "sWidth": "5%", "bSortable": false },
            { "mDataProp": "MaximumAllowedWithdrawal", "sTitle": "Maximum Allowed Withdrawal", "sWidth": "7%", "bSortable": false }
        ],
        "aaSorting": [[0, "desc"]],
        "initComplete": function(settings, json) {
            // Load the state of the header checkbox from local storage after DataTable initialization is complete
            $('#sheaderCheckbox').prop('checked', loadCheckboxState('sheaderCheckbox'));
        }
    });

    // Place the header checkbox anywhere on the grid
            $('#SavingsProductsGrid_wrapper .dataTables_scroll').prepend('<div><input type="checkbox" id="sheaderCheckbox"> Select $ Deselect All</div>');

    // Event listener for header checkbox
            $('#sheaderCheckbox').on('change', function () {
        // Check or uncheck all row checkboxes based on the header checkbox state
        $('.srowCheckbox').prop('checked', $(this).prop('checked')).each(function() {
            var checkboxKey = $(this).attr('id');
            // Save the state of the checkbox to local storage
            saveCheckboxState(checkboxKey, $(this).prop('checked'));
        });
        // Save the state of the header checkbox to local storage
                saveCheckboxState('sheaderCheckbox', $(this).prop('checked'));
    });

    // Event listener for individual row checkboxes
            $('#SavingsProductsGrid').on('change', '.srowCheckbox', function () {
        // Save the state of the checkbox to local storage
        var checkboxKey = $(this).attr('id');
        saveCheckboxState(checkboxKey, $(this).prop('checked'));

        // Uncheck header checkbox if any row checkbox is unchecked
        if (!$(this).prop('checked')) {
            $('#sheaderCheckbox').prop('checked', false);
            // Save the state of the header checkbox to local storage
            saveCheckboxState('sheaderCheckbox', false);
        }
    });
        });




        $('#submit-btn').click(function () {
            var selectedRowsData = [];
            $('.srowCheckbox:checked').each(function () {
                var rowData = {
                    Id: $(this).data('id'),
                    // Add other properties you want to include
                };
                selectedRowsData.push(rowData);
            });

            // Send the selected rows data to the backend using AJAX
            $.ajax({
                url: '/Accounts/DebitType/Create',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(selectedRowsData),
                success: function (response) {
                    // Handle success response
                },
                error: function (xhr, status, error) {
                    // Handle error response
                }
            });
        });
    </script>



    @*Loans Products*@
    <script>
        $(document).ready(function () {
    // Function to save the state of the header checkbox to local storage
    function saveCheckboxState(key, state) {
        // Implementation for saving checkbox state goes here
    }

    // Function to load the state of the header checkbox from local storage
    function loadCheckboxState(key) {
        // Implementation for loading checkbox state goes here
    }

    $(document).ready(function () {
        // Event listener for header checkbox
        $('#lheaderCheckbox').on('change', function () {
            // Check or uncheck all row checkboxes based on the header checkbox state
            $('.lrowCheckbox').prop('checked', $(this).prop('checked')).each(function () {
                var checkboxKey = $(this).attr('id');
                // Save the state of the checkbox to local storage
                saveCheckboxState(checkboxKey, $(this).prop('checked'));
            });
            // Save the state of the header checkbox to local storage
            saveCheckboxState('lheaderCheckbox', $(this).prop('checked'));
        });

        // Event listener for individual row checkboxes
        $('#LoanProductsGrid').on('change', '.lrowCheckbox', function () {
            // Save the state of the checkbox to local storage
            var checkboxKey = $(this).attr('id');
            saveCheckboxState(checkboxKey, $(this).prop('checked'));

            // Uncheck header checkbox if any row checkbox is unchecked
            if (!$(this).prop('checked')) {
                $('#lheaderCheckbox').prop('checked', false);
                // Save the state of the header checkbox to local storage
                saveCheckboxState('lheaderCheckbox', false);
            } else {
                // Check if all row checkboxes are checked and check the header checkbox accordingly
                var allChecked = $('.lrowCheckbox:checked').length === $('.lrowCheckbox').length;
                $('#lheaderCheckbox').prop('checked', allChecked);
                // Save the state of the header checkbox to local storage
                saveCheckboxState('lheaderCheckbox', allChecked);
            }
        });

        // Load the state of the header checkbox from local storage after the page is loaded
        $('#lheaderCheckbox').prop('checked', loadCheckboxState('lheaderCheckbox'));
    });

    // Initialize the DataTable
    var table = $('#LoanProductsGrid').DataTable({
        // Ajax settings
        "sPaginationType": "full_numbers",
        "bProcessing": true,
        "bServerSide": true,
        "sAjaxSource": "@(Url.Action("Index", "LoanProduct", new { Area = "Accounts"}))",

        "sServerMethod": "POST",
        // Callback settings
        "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
            var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
            var h = ("0" + createdDate.getHours()).slice(-2);
            var m = ("0" + createdDate.getMinutes()).slice(-2);
            var s = ("0" + createdDate.getSeconds()).slice(-2);
            var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
            aData.CreatedDate = dt_Created;

            //var editUrl = '<a href="/accounts/SavingsProduct/edit/' + aData.Id + '" class="btn btn-outline-info">Edit</a>';
            //var viewUrl = '<a href="/accounts/SavingsProduct/details/' + aData.Id + '" class="btn btn-outline-info">View</a>';
            var checkboxHTML = '<input type="checkbox" class="lrowCheckbox" data-id="' + aData.Id + '"';
            if (aData.AutomateLedgerFeeCalculation === true) {
                checkboxHTML += ' checked';
            }
            checkboxHTML += '>';

            $('td:eq(0)', nRow).html(checkboxHTML);

            return nRow;
        },
        // Display settings
        "aoColumns": [
            { "mDataProp": "Id", "sTitle": "Select", "sWidth": "0.01%" },
            { "mDataProp": "Description", "sTitle": "Name", "sWidth": "10%", "bSortable": false },
            { "mDataProp": "LoanRegistrationTermInMonths", "sTitle": "Term (Months)", "sWidth": "5%", "bSortable": false },
            { "mDataProp": "LoanInterestAnnualPercentageRate", "sTitle": "Annual Percentage Rate", "sWidth": "5%", "bSortable": false },
            { "mDataProp": "LoanRegistrationPaymentDueDateDescription", "sTitle": "Payment Due Date", "sWidth": "7%", "bSortable": false },
            { "mDataProp": "LoanRegistrationPaymentFrequencyPerYearDescription", "sTitle": "Payment Frequency Per Year", "sWidth": "7%", "bSortable": false }
        ],
        "aaSorting": [[0, "desc"]],
        "initComplete": function(settings, json) {
            // Load the state of the header checkbox from local storage after DataTable initialization is complete
            $('#lheaderCheckbox').prop('checked', loadCheckboxState('lheaderCheckbox'));
        }
    });

    // Place the header checkbox anywhere on the grid
    $('#LoanProductsGrid_wrapper .dataTables_scroll').prepend('<div><input type="checkbox" id="lheaderCheckbox"> Select $ Deselect All</div>');

    // Event listener for header checkbox
    $('#lheaderCheckbox').on('change', function () {
        // Check or uncheck all row checkboxes based on the header checkbox state
        $('.lrowCheckbox').prop('checked', $(this).prop('checked')).each(function() {
            var checkboxKey = $(this).attr('id');
            // Save the state of the checkbox to local storage
            saveCheckboxState(checkboxKey, $(this).prop('checked'));
        });
        // Save the state of the header checkbox to local storage
        saveCheckboxState('lheaderCheckbox', $(this).prop('checked'));
    });

    // Event listener for individual row checkboxes
    $('#LoanProductsGrid').on('change', '.lrowCheckbox', function () {
        // Save the state of the checkbox to local storage
        var checkboxKey = $(this).attr('id');
        saveCheckboxState(checkboxKey, $(this).prop('checked'));

        // Uncheck header checkbox if any row checkbox is unchecked
        if (!$(this).prop('checked')) {
            $('#lheaderCheckbox').prop('checked', false);
            // Save the state of the header checkbox to local storage
            saveCheckboxState('lheaderCheckbox', false);
        }
    });
        });




        $('#submit-btn').click(function () {
            var selectedRowsData = [];
            $('.lrowCheckbox:checked').each(function () {
                var rowData = {
                    Id: $(this).data('id'),
                    // Add other properties you want to include
                };
                selectedRowsData.push(rowData);
            });

            // Send the selected rows data to the backend using AJAX
            $.ajax({
                url: '/Accounts/DebitType/Create',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(selectedRowsData),
                success: function (response) {
                    // Handle success response
                },
                error: function (xhr, status, error) {
                    // Handle error response
                }
            });
        });
    </script>




    @*Investments Products*@
    <script>
        $(document).ready(function () {
    // Function to save the state of the header checkbox to local storage
    function saveCheckboxState(key, state) {
        // Implementation for saving checkbox state goes here
    }

    // Function to load the state of the header checkbox from local storage
    function loadCheckboxState(key) {
        // Implementation for loading checkbox state goes here
    }

    $(document).ready(function () {
        // Event listener for header checkbox
        $('#iheaderCheckbox').on('change', function () {
            // Check or uncheck all row checkboxes based on the header checkbox state
            $('.irowCheckbox').prop('checked', $(this).prop('checked')).each(function () {
                var checkboxKey = $(this).attr('id');
                // Save the state of the checkbox to local storage
                saveCheckboxState(checkboxKey, $(this).prop('checked'));
            });
            // Save the state of the header checkbox to local storage
            saveCheckboxState('iheaderCheckbox', $(this).prop('checked'));
        });

        // Event listener for individual row checkboxes
        $('#InvestmentsProductsGrid').on('change', '.irowCheckbox', function () {
            // Save the state of the checkbox to local storage
            var checkboxKey = $(this).attr('id');
            saveCheckboxState(checkboxKey, $(this).prop('checked'));

            // Uncheck header checkbox if any row checkbox is unchecked
            if (!$(this).prop('checked')) {
                $('#iheaderCheckbox').prop('checked', false);
                // Save the state of the header checkbox to local storage
                saveCheckboxState('iheaderCheckbox', false);
            } else {
                // Check if all row checkboxes are checked and check the header checkbox accordingly
                var allChecked = $('.irowCheckbox:checked').length === $('.irowCheckbox').length;
                $('#iheaderCheckbox').prop('checked', allChecked);
                // Save the state of the header checkbox to local storage
                saveCheckboxState('iheaderCheckbox', allChecked);
            }
        });

        // Load the state of the header checkbox from local storage after the page is loaded
        $('#iheaderCheckbox').prop('checked', loadCheckboxState('iheaderCheckbox'));
    });

    // Initialize the DataTable
            var table = $('#InvestmentsProductsGrid').DataTable({
        // Ajax settings
        "sPaginationType": "full_numbers",
        "bProcessing": true,
        "bServerSide": true,
        "sAjaxSource": "@(Url.Action("Index", "InvestmentProduct", new { Area = "Accounts"}))",

        "sServerMethod": "POST",
        // Callback settings
        "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
            var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
            var h = ("0" + createdDate.getHours()).slice(-2);
            var m = ("0" + createdDate.getMinutes()).slice(-2);
            var s = ("0" + createdDate.getSeconds()).slice(-2);
            var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
            aData.CreatedDate = dt_Created;

            //var editUrl = '<a href="/accounts/SavingsProduct/edit/' + aData.Id + '" class="btn btn-outline-info">Edit</a>';
            //var viewUrl = '<a href="/accounts/SavingsProduct/details/' + aData.Id + '" class="btn btn-outline-info">View</a>';
            var checkboxHTML = '<input type="checkbox" class="irowCheckbox" data-id="' + aData.Id + '"';
            if (aData.AutomateLedgerFeeCalculation === true) {
                checkboxHTML += ' checked';
            }
            checkboxHTML += '>';

            $('td:eq(0)', nRow).html(checkboxHTML);

            return nRow;
        },
        // Display settings
        "aoColumns": [
            { "mDataProp": "Id", "sTitle": "Select", "sWidth": "0.09%" },
            { "mDataProp": "Description", "sTitle": "Name", "sWidth": "10%", "bSortable": false },
            { "mDataProp": "MinimumBalance", "sTitle": "Minimum Balance", "sWidth": "7%", "bSortable": false },
            { "mDataProp": "MaximumBalance", "sTitle": "Maximum Balance", "sWidth": "7%", "bSortable": false },
            { "mDataProp": "AnnualPercentageYield", "sTitle": "Annual Percentage Yield", "sWidth": "5%", "bSortable": false },
            { "mDataProp": "ChartOfAccountName", "sTitle": "G/L Account Name", "sWidth": "15%", "bSortable": false }
        ],
        "aaSorting": [[0, "desc"]],
        "initComplete": function(settings, json) {
            // Load the state of the header checkbox from local storage after DataTable initialization is complete
            $('#iheaderCheckbox').prop('checked', loadCheckboxState('iheaderCheckbox'));
        }
    });

    // Place the header checkbox anywhere on the grid
            $('#InvestmentsProductsGrid_wrapper .dataTables_scroll').prepend('<div><input type="checkbox" id="iheaderCheckbox"> Select $ Deselect All</div>');

    // Event listener for header checkbox
    $('#iheaderCheckbox').on('change', function () {
        // Check or uncheck all row checkboxes based on the header checkbox state
        $('.irowCheckbox').prop('checked', $(this).prop('checked')).each(function() {
            var checkboxKey = $(this).attr('id');
            // Save the state of the checkbox to local storage
            saveCheckboxState(checkboxKey, $(this).prop('checked'));
        });
        // Save the state of the header checkbox to local storage
        saveCheckboxState('iheaderCheckbox', $(this).prop('checked'));
    });

    // Event listener for individual row checkboxes
            $('#InvestmentsProductsGrid').on('change', '.irowCheckbox', function () {
        // Save the state of the checkbox to local storage
        var checkboxKey = $(this).attr('id');
        saveCheckboxState(checkboxKey, $(this).prop('checked'));

        // Uncheck header checkbox if any row checkbox is unchecked
        if (!$(this).prop('checked')) {
            $('#iheaderCheckbox').prop('checked', false);
            // Save the state of the header checkbox to local storage
            saveCheckboxState('iheaderCheckbox', false);
        }
    });
        });




        $('#submit-btn').click(function () {
            var selectedRowsData = [];
            $('.rowCheckbox:checked').each(function () {
                var rowData = {
                    Id: $(this).data('id'),
                    // Add other properties you want to include
                };
                selectedRowsData.push(rowData);
            });

            // Send the selected rows data to the backend using AJAX
            $.ajax({
                url: '/Accounts/DebitType/Create',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(selectedRowsData),
                success: function (response) {
                    // Handle success response
                },
                error: function (xhr, status, error) {
                    // Handle error response
                }
            });
        });
    </script>
}
