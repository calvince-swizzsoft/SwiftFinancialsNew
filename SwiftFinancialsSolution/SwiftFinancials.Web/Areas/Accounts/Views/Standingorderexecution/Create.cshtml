@model Application.MainBoundedContext.DTO.AccountsModule.RecurringBatchDTO
@{
    var QueuePriority = ViewBag.QueuePriorityTypeSelectList as List<SelectListItem>;
    var chargeTypes = ViewBag.ChargeTypeSelectList as List<SelectListItem>;
    var Months = ViewBag.MonthsSelectList as List<SelectListItem>;
    var ChargeType = ViewBag.ChargeTypeSelectList as List<SelectListItem>;
    var ConcessionType = ViewBag.ConcessionTypeSelectList as List<SelectListItem>;
    ViewBag.Title = "StandingOrderExecution";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>Standing order execution</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                @Html.ActionLink("Dashboard", "Index", "Home", new { Area = "" }, null)
            </li>
            <li class="breadcrumb-item active">
                <strong>List</strong>
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="close-link">
                            <i class="fa fa-times"></i>
                        </a>
                    </div>
                </div>

                <div class="ibox-content bg-white">
                    @using (Html.BeginForm("Create", "StandingOrderExecution", new { Area = "Accounts" }, FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })


                            <div class="ibox-content">
                                <div class="sk-spinner sk-spinner-double-bounce">
                                    <div class="sk-double-bounce1"></div>
                                    <div class="sk-double-bounce2"></div>
                                </div>
                                <div class="row">

                                    <div class="col-lg-6">
                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.MonthDescription, new { @class = "control-label" })
                                            <div class="input-group">
                                                <span class="input-group-addon"><i class="fa fa-bars fa-fw"></i></span>
                                                @Html.DropDownListFor(model => model.Month, Months, new { @class = "form-control input-md" })
                                                @Html.ValidationMessageFor(model => model.Month)
                                            </div>
                                        </div>


                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.PostingPeriodId, htmlAttributes: new { @class = "control-label" })
                                            @Html.HiddenFor(model => model.PostingPeriodId)
                                            @Html.ValidationMessageFor(model => model.PostingPeriodId, "", new { @class = "text-danger" })
                                            @Html.EditorFor(model => model.PostingPeriodDescription, new { htmlAttributes = new { @class = "PostingPeriodsLookup form-control required", placeholder = "posting period Name" } })
                                        </div>

                                    </div>
                                    <div class="col-lg-6">


                                        <div class="form-group form-group-lg">
                                            @Html.LabelFor(model => model.Priority, new { @class = "control-label" })
                                            <div class="input-group">
                                                <span class="input-group-addon"><i class="fa fa-bars fa-fw"></i></span>
                                                @Html.DropDownListFor(model => model.Priority, QueuePriority, new { @class = "form-control input-md" })
                                                @Html.ValidationMessageFor(model => model.Priority)
                                            </div>
                                        </div>


                                        <div class="form-group form-group-lg">
                                            @Html.CheckBoxFor(model => model.EnforceMonthValueDate, htmlAttributes: new { @class = "" })
                                            @Html.LabelFor(model => model.EnforceMonthValueDate, new { htmlAttributes = new { @class = "form-control", @placeholder = "EnforceMonthValueDate?" } })
                                            @Html.ValidationMessageFor(model => model.EnforceMonthValueDate, "", new { @class = "text-danger" })
                                        </div>
                                    </div>

                                </div>


                            </div>
                            <input type="checkbox" id="headerCheckbox" style="transform: scale(1); color: blue;">
                            <label for="headerCheckbox" style="font-weight: bold; color: blue;">Select All</label>

                            @*<meta charset="UTF-8">
                                <meta name="viewport" content="width=device-width, initial-scale=1.0">*@

                            <style>
                                .tabcontent {
                                    display: none;
                                    padding: 20px;
                                    border-top: none;
                                }

                                .mini-button {
                                    background-color: #f1f1f1;
                                    border: none;
                                    color: black;
                                    padding: 8px 12px;
                                    text-align: center;
                                    text-decoration: none;
                                    display: inline-block;
                                    font-size: 14px;
                                    margin: 4px 2px;
                                    cursor: pointer;
                                    border-radius: 4px;
                                }

                                    .mini-button.active {
                                        background-color: #0D98BA;
                                    }
                            </style>


                            <div class="tab">
                                <button class="mini-button active" onclick="openTab(event, 'Tab1')">savingsProducts</button>
                                <button class="mini-button active" onclick="openTab(event, 'Tab2')">Loaning Products</button>
                                <button class="mini-button active" onclick="openTab(event, 'Tab3')">Employees Grid</button>
                                <button class="mini-button active" onclick="openTab(event, 'Tab4')">Investments Grid</button>
                            </div>

                            <div id="Tab1" class="tabcontent" style="display:block;">
                                <div class="table-responsive">
                                    <table id="savingsProductsGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                                        <tbody>
                                            <!-- Table body will be populated dynamically by DataTables -->
                                        </tbody>
                                    </table>
                                </div>
                            </div>

                            <div id="Tab2" class="tabcontent">
                                <div class="table-responsive">
                                    <table id="LoanProductsGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                                        <tbody>
                                            <!-- Table body will be populated dynamically by DataTables -->
                                        </tbody>
                                    </table>
                                </div>
                            </div>

                            <div id="Tab3" class="tabcontent">
                                <div class="table-responsive">
                                    <table id="employeesGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                                        <tbody>
                                            <!-- Table body will be populated dynamically by DataTables -->
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            <div id="Tab4" class="tabcontent">
                                <div class="table-responsive">
                                    <table id="InvestmentProductsGrid" class="table table-striped table-bordered table-hover" style="width:100%;">
                                        <tbody>
                                            <!-- Table body will be populated dynamically by DataTables -->
                                        </tbody>
                                    </table>
                                </div>
                            </div>




                            <div class="form-group form-group-sm">
                                <div class="col-md-offset-2 col-md-10">
                                    <input type="submit" value="Create" class="btn btn-primary" />
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles{
    @Styles.Render("~/Content/plugins/iCheck/iCheckStyles")
    @Styles.Render("~/plugins/wizardStepsStyles")
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/plugins/awesomeCheckboxStyles")
    @Styles.Render("~/plugins/clockpickerStyles")
    @Styles.Render("~/plugins/dateRangeStyles")
    @Styles.Render("~/Content/plugins/chosen/chosenStyles")
    @Styles.Render("~/plugins/switcheryStyles")
    @Styles.Render("~/plugins/jasnyBootstrapStyles")
    @Styles.Render("~/plugins/nouiSliderStyles")
    @Styles.Render("~/plugins/dataPickerStyles")
    @Styles.Render("~/Content/plugins/ionRangeSlider/ionRangeStyles")
    @Styles.Render("~/plugins/imagecropperStyles")
    @Styles.Render("~/Content/plugins/colorpicker/colorpickerStyles")
    @Styles.Render("~/plugins/select2Styles")
    @Styles.Render("~/plugins/touchSpinStyles")
    @Styles.Render("~/plugins/tagInputsStyles")
    @Styles.Render("~/plugins/duallistStyles")
}

@section Scripts{
    @Scripts.Render("~/plugins/wizardSteps")
    @Scripts.Render("~/plugins/validate")
    @Scripts.Render("~/plugins/typehead")
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/iCheck")
    @Scripts.Render("~/plugins/dataPicker")
    @Scripts.Render("~/plugins/ionRange")
    @Scripts.Render("~/plugins/nouiSlider")
    @Scripts.Render("~/plugins/jasnyBootstrap")
    @Scripts.Render("~/plugins/switchery")
    @Scripts.Render("~/plugins/chosen")
    @Scripts.Render("~/plugins/knob")
    @Scripts.Render("~/plugins/imagecropper")
    @Scripts.Render("~/plugins/colorpicker")
    @Scripts.Render("~/plugins/clockpicker")
    @Scripts.Render("~/plugins/dateRange")
    @Scripts.Render("~/plugins/select2")
    @Scripts.Render("~/plugins/touchSpin")
    @Scripts.Render("~/plugins/tagInputs")
    @Scripts.Render("~/plugins/duallist")




    <script type="text/javascript">

        function openTab(evt, tabName) {
                                    var i, tabcontent, tablinks;
        tabcontent = document.getElementsByClassName("tabcontent");
        for (i = 0; i < tabcontent.length; i++) {
            tabcontent[i].style.display = "none";
                                    }
        tablinks = document.getElementsByClassName("mini-button");
        for (i = 0; i < tablinks.length; i++) {
            tablinks[i].className = tablinks[i].className.replace(" active", "");
                                    }
        document.getElementById(tabName).style.display = "block";
        evt.currentTarget.className += " active";
                                }

        // Event listener for header checkbox
        $('#headerCheckbox').on('change', function () {
            // Check or uncheck all row checkboxes based on the header checkbox state
            $('.tabcontent:visible .rowCheckbox').prop('checked', $(this).prop('checked')).each(function () {
                var checkboxKey = $(this).attr('id');
                // Save the state of the checkbox to local storage
                saveCheckboxState(checkboxKey, $(this).prop('checked'));
            });
            // Save the state of the header checkbox to local storage
            saveCheckboxState('headerCheckbox', $(this).prop('unchecked'));
        });


    // Initialize the DataTable
    var table = $('#LoanProductsGrid').DataTable({
        // Ajax settings
        "sPaginationType": "full_numbers",
        "bProcessing": true,
        "bServerSide": true,
        "sAjaxSource": "@(Url.Action("Index", "LoanProduct", new { Area = "Accounts"}))",

        "sServerMethod": "POST",
        // Callback settings
        "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
            var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
            var h = ("0" + createdDate.getHours()).slice(-2);
            var m = ("0" + createdDate.getMinutes()).slice(-2);
            var s = ("0" + createdDate.getSeconds()).slice(-2);
            var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
            aData.CreatedDate = dt_Created;

            var editUrl = '<a href="/accounts/SavingsProduct/edit/' + aData.Id + '" class="btn btn-outline-info">Edit</a>';
            var viewUrl = '<a href="/accounts/SavingsProduct/details/' + aData.Id + '" class="btn btn-outline-info">View</a>';
            var checkboxHTML = '<input type="checkbox" class="rowCheckbox" data-id="' + aData.Id + '"';
            if (aData.AutomateLedgerFeeCalculation === true) {
                checkboxHTML += ' checked';
            }
            checkboxHTML += '>';

            $('td:eq(0)', nRow).html(checkboxHTML);

            return nRow;
        },
        // Display settings
        "aoColumns": [
            { "mDataProp": "Id", "sTitle": "Select", "sWidth": "10%" },
            { "mDataProp": "LoanRegistrationLoanProductSectionDescription", "sTitle": "Loan Product Section", "sWidth": "15%" },
            { "mDataProp": "LoanRegistrationTermInMonths", "sTitle": "Term in Months", "sWidth": "15%" },
            { "mDataProp": "LoanRegistrationLoanProductCategoryDescription", "sTitle": "category", "sWidth": "15%" },
            { "mDataProp": "Description", "sTitle": "Loan Product Name", "sWidth": "15%", "bSortable": false }
        ],
        "aaSorting": [[0, "desc"]],
        "initComplete": function(settings, json) {
            // Load the state of the header checkbox from local storage after DataTable initialization is complete
            $('#headerCheckbox').prop('checked', loadCheckboxState('headerCheckbox'));
        }
    });

    // Place the header checkbox anywhere on the grid
    $('#LoanProductsGrid_wrapper .dataTables_scroll').prepend('<div><input type="checkbox" id="headerCheckbox"> Select $ Deselect All</div>');

    // Event listener for header checkbox
    $('#headerCheckbox').on('change', function () {
        // Check or uncheck all row checkboxes based on the header checkbox state
        $('.rowCheckbox').prop('checked', $(this).prop('checked')).each(function() {
            var checkboxKey = $(this).attr('id');
            // Save the state of the checkbox to local storage
            saveCheckboxState(checkboxKey, $(this).prop('checked'));
        });
        // Save the state of the header checkbox to local storage
        saveCheckboxState('headerCheckbox', $(this).prop('checked'));
    });

    // Event listener for individual row checkboxes
    $('#LoanProductsGrid').on('change', '.rowCheckbox', function () {
        // Save the state of the checkbox to local storage
        var checkboxKey = $(this).attr('id');
        saveCheckboxState(checkboxKey, $(this).prop('checked'));

        // Uncheck header checkbox if any row checkbox is unchecked
        if (!$(this).prop('checked')) {
            $('#headerCheckbox').prop('checked', false);
            // Save the state of the header checkbox to local storage
            saveCheckboxState('headerCheckbox', false);
        }
    });



    $('#employeesGrid').DataTable({
                // Ajax settings
                "sPaginationType": "full_numbers",
                "bProcessing": true,
                "bServerSide": true,
                "sAjaxSource": "@(Url.Action("Index", "Employee", new { Area = "HumanResource"}))",
                "sServerMethod": "POST",
                // Callback settings
                "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {

                   var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
                    var h = ("0" + createdDate.getHours()).slice(-2);
                    var m = ("0" + createdDate.getMinutes()).slice(-2);
                    var s = ("0" + createdDate.getSeconds()).slice(-2);

                    var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
                    aData.CreatedDate = dt_Created;


                    var checkbox = '<input type="checkbox" class="rowCheckbox" data-id="' + aData.Id + '"';
                    $('td:eq(0)', nRow).html(checkbox);
                    $('td:eq(4)', nRow).html(dt_Created);
                  //  $('td:eq(5)', nRow).html(viewUrl + '&nbsp;' + editUrl);

                    return nRow;
                },
          // Display settings
          "aoColumns": [
            { "mDataProp": "Id", "sTitle": "Name", "sWidth": "15%" },
              { "mDataProp": "Customer.FullName", "sTitle": "Branch Name", "sWidth": "10%" },
            { "mDataProp": "DepartmentDescription", "sTitle": "DepartmentDescription", "sWidth": "15%" },
            { "mDataProp": "IsLocked", "sTitle": "Is Locked?", "sWidth": "15%" },
            { "mDataProp": "CreatedDate", "sTitle": "", "sWidth": "10%", "bSortable": false }],
          "aaSorting": [[0, "desc"]]
        });


        // Initialize the DataTable for Savings Products
        $('#savingsProductsGrid').DataTable({
            // Ajax settings
            "sPaginationType": "full_numbers",
            "bProcessing": true,
            "bServerSide": true,
            "sAjaxSource": "@(Url.Action("Index", "SavingsProduct", new { Area = "Accounts"}))",
            "sServerMethod": "POST",
            // Callback settings
            "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
                var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
                var h = ("0" + createdDate.getHours()).slice(-2);
                var m = ("0" + createdDate.getMinutes()).slice(-2);
                var s = ("0" + createdDate.getSeconds()).slice(-2);
                var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
                aData.CreatedDate = dt_Created;

                var checkbox = '<input type="checkbox" class="rowCheckbox" id="checkbox_' + aData.Id + '">';
                $('td:eq(0)', nRow).html(checkbox);
                $('td:eq(4)', nRow).html(dt_Created);

                return nRow;
            },
            // Display settings
            "aoColumns": [
                { "mDataProp": "Id", "sTitle": "Select", "sWidth": "5%" },
                { "mDataProp": "Description", "sTitle": "Name", "sWidth": "5%" },
                { "mDataProp": "IsLocked", "sTitle": "Is Locked", "sWidth": "5%" },
                { "mDataProp": "IsMandatory", "sTitle": "Is Mandatory", "sWidth": "5%" },
                { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "10%", "bSortable": false }
            ],
            "aaSorting": [[0, "desc"]]
        });


       // Initialize the DataTable for Savings Products
    $('#InvestmentProductsGrid').DataTable({
            // Ajax settings
            "sPaginationType": "full_numbers",
            "bProcessing": true,
            "bServerSide": true,
            "sAjaxSource": "@(Url.Action("Index", "InvestmentProduct", new { Area = "Accounts"}))",
            "sServerMethod": "POST",
            // Callback settings
            "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
                var createdDate = new Date(parseInt(aData.CreatedDate.replace("/Date(", "").replace(")/", ""), 10));
                var h = ("0" + createdDate.getHours()).slice(-2);
                var m = ("0" + createdDate.getMinutes()).slice(-2);
                var s = ("0" + createdDate.getSeconds()).slice(-2);
                var dt_Created = $.datepicker.formatDate('dd/mm/yy', createdDate) + " " + h + ":" + m + ":" + s;
                aData.CreatedDate = dt_Created;

                var checkbox = '<input type="checkbox" class="rowCheckbox" id="checkbox_' + aData.Id + '">';
                $('td:eq(0)', nRow).html(checkbox);
                $('td:eq(4)', nRow).html(dt_Created);

                return nRow;
            },
            // Display settings
            "aoColumns": [
                { "mDataProp": "Id", "sTitle": "Select", "sWidth": "15%" },
                { "mDataProp": "ChartOfAccountAccountName", "sTitle": "ChartOfAccount", "sWidth": "15%" },
                { "mDataProp": "IsLocked", "sTitle": "IsLocked", "sWidth": "10%" },
                { "mDataProp": "IsMandatory", "sTitle": "IsMandatory", "sWidth": "10%" },
                { "mDataProp": "CreatedDate", "sTitle": "Created Date", "sWidth": "10%", "bSortable": false }],
            "aaSorting": [[0, "desc"]]
        });


    // Event listener for header checkbox
    $('#headerCheckbox').on('change', function () {
        // Check or uncheck all row checkboxes based on the header checkbox state
        $('.rowCheckbox').prop('checked', $(this).prop('checked')).each(function () {
            var checkboxKey = $(this).attr('id');
            // Save the state of the checkbox to local storage
            saveCheckboxState(checkboxKey, $(this).prop('checked'));
        });
        // Save the state of the header checkbox to local storage
        saveCheckboxState('headerCheckbox', $(this).prop('checked'));
    });

    // Event listener for individual row checkboxes
    $('#savingsProductsGrid').on('change', '.rowCheckbox', function () {
        // Save the state of the checkbox to local storage
        var checkboxKey = $(this).attr('id');
        saveCheckboxState(checkboxKey, $(this).prop('checked'));

        // Uncheck header checkbox if any row checkbox is unchecked
        if (!$(this).prop('checked')) {
            $('#headerCheckbox').prop('checked', false);
            // Save the state of the header checkbox to local storage
            saveCheckboxState('headerCheckbox', false);
        }
    });


        // PostingPeriod typehead starts here
        $('.PostingPeriodsLookup').typeahead({
            source: function (query, process) {
                return $.getJSON(
                    '@Url.Action("GetPostingPeriodsAsync", "PostingPeriod", new { Area = "Accounts" })',
                    { query: query },
                    function (PostingPeriods) {
                        var newPostingPeriodsData = [];
                        PostingPeriodsMap = {};
                        $.each(PostingPeriods, function (i, PostingPeriod) {
                            newPostingPeriodsData.push(PostingPeriod.Description);
                            PostingPeriodsMap[PostingPeriod.Description] = PostingPeriod;
                        });
                        process(newPostingPeriodsData);
                        return;
                    });
            },
            matcher: function (item) {
                if (item.toLowerCase().indexOf(this.query.trim().toLowerCase()) != -1) {
                    return true;
                }
            },
            sorter: function (items) {
                return items.sort();
            },
            highlighter: function (item) {
                var regex = new RegExp('(' + this.query + ')', 'gi');
                return item.replace(regex, "<strong>$1</strong>");
            },
            updater: function (item) {
                var postingPeriodId = "";
                PostingPeriodId = PostingPeriodsMap[item].Id;
                $('#PostingPeriodId').val(PostingPeriodId);
                return item;
            }
        });


    $('#createButton').click(function () {
        var selectedRowsData = {
            recurringBatchDTO: {
                // Populate recurringBatchDTO properties if necessary
            },
            selectedRows: [],
            selectedRows1: [],
            selectedRows2: [],
            selectedRows3: []
        };

        // Determine the currently active tab
        var activeTabId = $('.tab .active').attr('id');

        // Populate selectedRowsData from the corresponding grid based on the active tab
        $('#' + activeTabId + ' .rowCheckbox:checked').each(function () {
            var rowData = {
                Id: $(this).data('id'),
                // Add other properties you want to include
            };
            // Determine which grid this row belongs to and push it accordingly
            switch (activeTabId) {
                case 'Tab1':
                    selectedRowsData.selectedRows.push(rowData);
                    break;
                case 'Tab2':
                    selectedRowsData.selectedRows1.push(rowData);
                    break;
                case 'Tab3':
                    selectedRowsData.selectedRows2.push(rowData);
                    break;
                case 'Tab4':
                    selectedRowsData.selectedRows3.push(rowData);
                    break;
            }
        });

        // Send the selected rows data to the backend using AJAX
        $.ajax({
            url: '/Standingorderexecution/Create', // Update this URL to point to your controller action
            type: 'POST',
            contentType: 'application/json',
            data: JSON.stringify(selectedRowsData),
            success: function (response) {
                // Handle success response
            },
            error: function (xhr, status, error) {
                // Handle error response
            }
        });
    });


    </script>
}
