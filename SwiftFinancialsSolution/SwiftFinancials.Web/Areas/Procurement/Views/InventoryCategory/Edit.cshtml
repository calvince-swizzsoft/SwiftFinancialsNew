@model Application.MainBoundedContext.DTO.InventoryCatalogDTO

@{
    ViewBag.Title = "Edit Inventory Item";
}

<h2>Edit Inventory Item</h2>

@using (Html.BeginForm("Edit", "InventoryCatalog", FormMethod.Post, new { area = "Procurement" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.Id)

    <div class="form-horizontal">
        <hr />

        @* Sample Fields (repeat structure for all your fields) *@
        @foreach (var prop in typeof(Application.MainBoundedContext.DTO.InventoryCatalogDTO).GetProperties())
        {
            if (prop.Name != "Id")
            {
                <div class="form-group">
                    @Html.Label(prop.Name, new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.Editor(prop.Name, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessage(prop.Name, "", new { @class = "text-danger" })
                    </div>
                </div>
            }
        }

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save Changes" class="btn btn-primary" />
                @Html.ActionLink("Back to List", "Index", "InventoryCatalog", new { area = "Procurement" }, new { @class = "btn btn-default" })
            </div>
        </div>
    </div>
}
